# extracted artifacts — large, version- and machine-specific
extract/
# the build/ output dir (OUT_DIR): artifacts, and make harness's reports
/build/
extracted/
*.node
*.orig
*.bak

# the processed CLI and any local install output
cli.original.js
cli.original.cjs
cli.cjs
# the generated sibling shim (postprocess.py), not a source file
cli.js

# python
__pycache__/
*.pyc

# macOS
.DS_Store
