// bun-ant-cell-segmenter.mjs - pure-JavaScript port of Bun.ant.CellSegmenter, the native class that
// Claude Code 2.1.280's Ink renderer gets from its private Bun fork (bun-internal 1.4.3), so that
// the renderer runs on a stock Bun 1.3.14 or on Node >= 22. No dependencies, no Bun APIs.
//
//   import { CellSegmenter, installBunAnt } from "./bun-ant-cell-segmenter.mjs";
//   installBunAnt(globalThis.Bun);   // defines Bun.ant.CellSegmenter unless a native one exists
//
// Behaviour is byte-for-byte that of the native implementation, as characterised with an in-process
// oracle (docs/findings.md 14) and verified differentially
// (tests/test_cell_segmenter.py; scripts/harness.py --only segmenter). In short:
//   * segment(): UTF-16 tokenizer for CSI/OSC/DCS/SOS/PM/APC and C1 forms; SGR state machine with
//     per-slot replacement and opaque sequences; OSC 8 links; uucode-style grapheme clustering with
//     Unicode 16 break classes and Unicode 17 widths (embedded table measured on the native);
//     interning into graphemes/sgrKeys/sgrCloseKeys/uris (same Array objects for life, mirrored on
//     return); the capacity rules for short calls (prefix writes, ASCII all-or-nothing block,
//     kernel-unit interning); reordered=true runs a port of ICU 78.3 ubidi on the UTF-16 bidi text
//     and places each cell at the visual position of its cluster's first code unit - only when no
//     cell overflowed, some written cell starts with an R/AL code point (RLM included) and the raw
//     input holds a UTF-16 unit in the RTL blocks (0590-08FF, FB1D-FDFF, FE70-FEFE, D802/D803/
//     D83A/D83B lead surrogates), escape payloads and substituted code points included.
//   * paint() and setCell(): screen cell writes, spacer heads/tails, orphan cleanup, damage.
// Generated by build.js from cell-segmenter.src.mjs and tables.generated.js (build-tables.js).

// ---- generated by build-tables.js (bidi 17.0.0); do not edit ----
const PROPS = [36865,45058,40961,45057,49153,299011,180480,184576,151808,147712,160000,143616,131328,186624,153856,53592,133376,36864,55640,145664,295024,33689856,24584,33693952,33562624,155904,33562632,24840,262424,384,262168,295032,0,262256,128,264,262512,544,40,48,32768,299088,8687688,33558528,172288,57344,65536,73728,61440,69632,54616,77824,81920,86016,90112,2392176,56664,1368,54872,184832,3416,54624,53848,344,56672,54880,180736,131584,262768,600,568,576,168192,17072240,160256,152064,147968,143872,296,1624,1048848,4249192,299120];
const ID_RLE = "9.0,.1,.2,.3,.4,.2,d.0,.5,3.2,.3,.6,2.7,3.8,5.7,.9,.a,.9,2.a,a.b,.a,6.7,q.c,6.7,q.c,4.7,6.0,.2,a.0,.5,7.0,.5,2.0,.5,.0,3.5,.a,.d,2.8,.e,.8,.7,2.d,.f,.g,2.7,.h,.i,.7,2.e,2.j,.d,.c,3.d,.j,.g,.7,4.d,6.c,.g,9.c,.g,6.c,.d,.g,5.c,4.g,4.c,.g,.c,3.g,.c,2.g,2.c,.g,.c,2.g,3.c,.d,3.g,.c,.g,.c,.g,2.c,.g,f.c,.g,.c,.g,7.c,.g,a.c,2.g,3.c,.g,5.c,3.g,4.c,.g,6.c,4.g,.c,.g,3.c,4.g,.c,.g,4.c,2.g,i.c,2.g,3.c,.g,2q.c,.g,.c,.g,.c,.g,.c,.g,.c,.g,.c,.g,.c,.g,.c,.g,38.c,.g,f.c,.g,2f.c,2.7,7.c,2.7,.d,2.7,.d,.7,3.d,.7,.d,2.7,.g,.c,6.7,4.d,.7,.d,.7,.d,5.c,9.7,.c,h.7,34.k,4.c,2.7,8.c,.7,5.c,2.7,.c,.7,9.c,h.g,.c,7.g,7.c,h.g,.c,7.g,18.c,.7,a.c,.g,e.c,1s.g,.c,.g,1d.c,7.k,74.c,.7,2.c,2.7,.8,.l,19.k,.l,.k,.l,2.k,.l,2.k,.l,.k,1k.l,6.m,2.7,.n,2.8,.n,.a,.n,2.7,b.k,.n,.o,1a.n,l.k,a.p,.8,2.p,3.n,.k,2t.n,7.k,.m,.7,6.k,2.n,2.k,.7,4.k,2.n,a.b,l.n,.q,.n,.k,u.n,r.k,2j.n,b.k,f.n,17.l,9.k,2.l,4.7,3.l,.k,o.l,4.k,.l,9.k,.l,3.k,.l,5.k,17.l,3.k,4.l,1c.n,2.r,5.n,9.k,16.n,o.k,.m,w.k,.s,h.c,11.t,.k,.u,.k,.c,3.u,8.k,4.u,.v,2.s,.c,7.k,8.t,2.c,2.k,k.c,8.t,.w,.k,.u,.s,h.c,k.t,.c,7.t,.c,.t,3.c,4.t,2.w,.k,.c,.x,2.u,4.k,2.w,2.u,2.w,2.u,.v,3.c,6.w,.x,4.c,2.t,.c,.t,2.c,2.k,c.c,2.t,2.8,7.c,.8,2.c,.k,.c,.w,2.k,.s,1i.c,2.w,.k,.c,3.u,2.k,4.w,2.k,2.w,3.k,3.c,.k,6.w,a.c,2.w,c.c,2.k,3.c,.k,a.c,.w,2.k,.s,h.c,k.t,.c,7.t,.c,2.t,.c,5.t,2.w,.k,.c,3.u,5.k,.w,2.k,.u,.w,2.u,.v,3.c,7.w,a.c,2.k,d.c,.8,7.c,.t,6.k,.w,.k,.u,.s,h.c,k.t,.c,7.t,.c,2.t,.c,5.t,2.w,.k,.c,.x,.k,.u,4.k,2.w,2.u,2.w,2.u,.v,3.c,4.w,2.k,.x,4.c,2.t,.c,.t,2.c,2.k,d.c,.t,e.c,2.w,.k,1j.c,3.w,.c,.x,.u,.k,2.u,3.w,3.u,.w,3.u,.k,3.c,6.w,.x,a.c,2.w,f.c,6.7,.8,.7,5.c,.k,2.u,.s,.k,g.c,k.t,.c,g.t,2.w,.k,.c,3.k,4.u,.w,3.k,.w,3.k,.v,3.c,4.w,2.k,.w,3.t,7.c,2.k,k.c,7.7,.c,.w,.k,.u,.s,1i.c,2.w,.k,.c,.u,2.x,.u,.x,2.u,.w,3.x,.w,2.x,2.k,3.c,4.w,2.x,.w,a.c,2.k,f.c,.s,c.c,2.k,.u,.s,h.c,11.t,.y,2.k,.c,.x,2.u,4.k,.w,3.u,.w,3.u,.v,.z,8.c,.10,a.c,2.k,t.c,.k,2.s,1y.c,.k,4.c,.10,2.s,3.k,.c,.k,.c,7.s,.10,i.c,2.s,1p.c,.k,.c,.s,7.k,4.c,.8,7.c,8.k,2q.c,.k,.c,.s,9.k,b.c,7.k,21.c,2.k,r.c,.k,.c,.k,.c,.k,4.7,2.s,1d.c,e.k,.s,5.k,.c,2.k,5.c,b.k,.c,10.k,9.c,.k,2u.c,4.k,.s,6.k,.c,2.k,2.s,2.k,n.c,2.s,2.k,4.c,3.k,g.c,4.k,d.c,.k,.c,.s,2.k,6.c,.k,f.c,.k,2q.c,2o.11,20.12,2g.13,9p.c,3.k,1c.c,a.7,2u.c,.7,hr.c,.6,q.c,2.7,39.c,3.k,.10,s.c,2.k,.10,t.c,2.k,u.c,2.k,1s.c,2.k,.s,7.k,8.s,.k,2.s,b.k,7.c,.8,.c,.k,i.c,a.7,6.c,b.7,3.k,.h,.k,39.c,2.k,y.c,.k,3a.c,3.k,4.s,2.k,3.s,4.c,2.s,.k,6.s,3.k,4.c,.7,3.c,2.7,48.c,y.7,n.c,2.k,2.s,.k,1l.c,.s,.k,.s,7.k,.c,.k,.c,.k,2.c,8.k,6.s,a.k,2.c,.k,1c.c,v.k,f.14,2.w,c.14,k.w,4.k,.s,1b.c,.k,.10,5.k,.10,.k,.10,4.s,.k,2.10,12.c,9.k,c.c,2.k,.s,u.c,.s,4.k,2.s,2.k,.10,3.k,1k.c,.k,.s,2.k,3.s,.k,.s,3.k,2.10,1c.c,8.s,8.k,2.s,2.k,48.c,3.k,.c,d.k,.s,7.k,4.c,.k,6.c,.k,2.c,.s,2.k,5i.c,1s.k,cd.c,.7,.c,3.7,b.c,3.7,d.c,3.7,d.c,3.7,d.c,2.7,.c,b.6,.h,.15,.16,.w,.17,.d,2.7,4.d,.7,2.d,2.7,2.d,2.7,3.d,.7,4.d,.6,.18,.19,.1a,.1b,.1c,.1d,.a,.e,.8,2.e,.8,.d,5.7,.d,.1e,.7,.d,5.7,.a,4.7,.1e,l.7,.6,6.h,.1f,.1g,.1h,.1i,6.h,.b,3.c,.j,5.b,2.9,3.7,.g,.b,4.j,5.b,2.9,3.7,h.c,c.8,.e,z.8,j.k,.1j,d.k,f.w,2.7,.c,.d,.7,.d,.7,.c,.7,.d,9.c,.g,.7,.c,.d,2.7,5.c,3.7,.d,.1k,.7,.c,.7,.g,.7,.c,.7,.c,.g,2.c,.8,a.c,.1l,2.7,4.c,5.7,5.c,4.7,2.c,3.7,2.d,6.7,4.d,.7,c.g,4.c,a.g,f.c,.d,2.7,4.c,4.d,6.1k,f.7,2.1e,d.7,2.d,o.7,.d,.7,.d,i.7,.d,o.7,.d,.7,2.d,3.7,2.d,2.7,.d,3.7,.d,.7,.d,.9,.8,.7,.d,4.7,.d,2.7,4.d,2.7,.d,.7,.d,.7,6.d,.7,.d,5.7,4.d,4.7,2.d,a.7,.d,3.7,.d,5.7,.d,d.7,2.d,2.7,4.d,2.7,2.d,2.7,2.d,i.7,2.d,2.7,2.d,d.7,.d,3.7,.d,b.7,.d,p.7,.d,2a.7,.d,7.7,2.1m,c.7,.1e,2.1n,b.7,1x.c,d.7,.f,c.7,.c,1l.7,.1e,p.7,4.1m,3.1e,.1m,2.1e,.1m,4.7,3.1e,1b.7,m.c,b.7,l.c,14.d,k.j,12.g,.1o,13.g,.7,2p.d,4.7,10.d,c.7,g.d,2.7,4.d,a.7,2.d,.7,7.d,2.1e,6.7,2.d,2.7,.1k,.d,4.7,2.d,2.7,.1k,.d,4.7,3.d,2.7,.d,2.7,4.d,g.7,4.d,9.7,.d,b.7,2.1e,2.1m,.7,5.1e,.i,.d,2.f,.i,4.f,.1k,.i,.f,.1e,.f,.7,2.1m,2.f,.1e,3.f,.i,.1p,.i,.f,.1e,.f,2.1e,2.f,.1e,3.f,.1e,3.f,2.1e,8.1q,3.1e,5.f,.1k,.f,.1k,5.f,c.1m,b.f,.1e,.1k,.i,.f,.1k,.i,.1k,.1e,.i,.1k,2.i,.f,2.i,.f,.i,b.f,.1e,2.f,.1e,.1m,6.f,4.7,6.1n,2.f,.1e,.1m,4.1e,.f,.1e,.f,2.1e,.f,2.i,.1e,.1m,5.f,.1e,2.f,2.1m,.1r,3.f,2.1e,b.f,2.1m,.i,4.f,2.1m,2.i,.1k,5.i,.1m,.1k,.i,.1k,.i,.1k,.1m,d.i,.f,.i,4.f,.i,.1k,.1m,5.i,2.1k,2.1m,.1k,.1m,.i,2.1k,.1s,.1m,2.i,.1m,2.i,2.f,.1e,2.f,.1m,2.7,2.1e,2.1t,2.1p,.f,.1e,2.f,.1e,.7,.1e,.7,.1e,6.7,.1e,3.7,.1e,6.7,.1m,a.7,2.1e,8.7,.d,6.7,.1e,2.7,.1e,4.7,.1m,.7,.1m,4.7,3.1m,.7,.1m,b.7,2.1e,3.f,e.7,a.d,l.7,3.1m,9.7,.1e,e.7,.1m,e.7,.1m,1s.7,74.c,1g.7,2.1e,cv.7,3.1e,j.7,2.1m,1f.7,.1m,4.7,.1m,4.d,q.7,2.c,3u.7,6d.c,6.7,4.c,3.k,7.c,7.7,3j.c,.k,2o.c,w.k,2m.7,y.c,q.1n,.c,2h.1n,c.c,5y.1n,q.c,g.1n,.1u,4.1n,3.1v,p.1n,9.1v,4.k,2.1w,.1q,5.1v,2.1n,5.1v,.1q,.1n,.7,.c,2e.1v,2.c,2.k,2.1n,3.1v,.1n,2i.1v,.1n,4.1v,5.c,17.1v,.c,2m.1v,.c,1c.1v,12.1n,9.c,.1n,19.1v,2.1n,.c,14.1v,8.g,g.1n,s.1v,3.1n,o.1v,.1x,.1v,.1x,n.1v,f.1n,c.1v,4.1n,4n.1v,4.1n,2r.1v,2.1n,v.1v,.1n,534.1v,1s.1n,h3h.1v,3.c,1j.1n,92.c,3.7,2n.c,4.k,.7,a.k,2.7,u.c,2.k,28.c,2.k,e.c,y.7,2u.c,.7,3d.c,.k,3.c,.k,4.c,.k,n.c,2.s,2.k,.s,4.7,.k,b.c,2.8,1m.c,4.7,8.c,2.s,1e.c,g.s,2.k,q.c,i.k,d.c,.k,12.c,8.k,p.c,b.k,.s,.10,c.c,t.11,3.c,3.k,.s,1b.c,.k,2.s,4.k,2.s,2.k,2.s,.10,10.c,.k,1v.c,6.k,2.s,2.k,2.s,2.k,c.c,.k,8.c,.k,.s,1a.c,.k,1f.c,.k,.c,3.k,2.c,2.k,5.c,2.k,.c,.k,15.c,.s,2.k,2.s,5.c,.s,.k,37.c,2.7,3b.c,2.s,.k,2.s,.k,2.s,.c,.s,.k,i.c,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,.1y,r.1z,c.c,n.12,4.w,1d.13,1l0.w,4xs.g,e8.1v,t.c,.l,.k,a.l,.9,12.l,37.n,g.7,a3.n,i.7,1s.n,2.7,1i.n,8.7,w.20,d.n,3.7,f.k,.21,a.1n,6.c,g.k,w.1n,.22,.1n,.22,.c,.1n,.22,9.1n,.23,2.1n,2.24,3.1n,.c,.1n,2.23,.1n,4.c,3z.n,.h,.c,2.1n,3.23,5.1n,.24,.22,.24,2.22,a.25,.22,6.1n,q.1v,6.1n,q.1v,6.1n,5.7,1k.c,2.10,1s.c,2.23,3.1n,2.23,.c,7.7,.c,9.20,4.7,.d,2.20,75.c,.7,1q.c,25.7,3.c,d.7,3.c,.7,2k.c,.k,6a.c,.k,r.b,3e.c,5.k,w5.c,7z.l,.7,69.l,3.k,.l,2.k,5.l,4.k,14.l,3.k,4.l,.k,4l.l,2.k,2a.l,7.7,cg.l,10.n,4.k,8.n,a.p,6.n,a.p,v.l,5.k,.7,6p.l,v.p,18.l,2.k,j.l,g.n,9.7,x.n,2.14,4.k,1c.l,m.n,b.k,v.n,i.l,4.k,3e.l,.s,.k,.s,1h.c,f.k,b.c,k.7,a.c,.k,2.c,2.k,a.c,3.k,.s,19.c,3.s,4.k,2.s,2.k,2.c,.z,4.c,.k,a.c,.z,1e.c,3.k,10.c,5.k,.s,8.k,g.c,2.s,18.c,.k,c.c,2.k,.s,1c.c,3.s,9.k,.s,.10,.c,2.z,5.c,4.k,.c,.s,.k,2k.c,3.s,3.k,2.s,.k,.10,2.k,6.c,.k,2.c,.k,4d.c,.k,3.s,8.k,l.c,2.k,2.s,1j.c,2.k,.c,.10,.s,.k,4.s,2.c,2.s,2.c,2.s,.10,9.c,.10,a.c,2.s,2.c,7.k,3.c,5.k,1v.c,.10,2.s,6.k,.c,.10,2.c,.10,.c,3.10,.s,.c,2.s,.k,.10,.k,.z,.k,e.c,2.k,2a.c,3.s,8.k,2.s,3.k,.s,.k,n.c,.k,29.c,.10,2.s,6.k,.s,.k,2.s,.10,.s,2.k,.s,2.k,6j.c,.10,2.s,4.k,2.c,4.s,2.k,.s,2.k,r.c,2.k,2a.c,3.s,8.k,2.s,.k,.s,2.k,v.c,d.7,1q.c,.k,.s,.k,2.s,6.k,.10,.k,2t.c,.k,.s,.k,2.c,4.k,.s,5.k,74.c,3.s,9.k,.s,2.k,6t.c,.10,5.s,.c,2.s,2.c,2.k,.10,.k,.z,.s,.z,.s,.k,3x.c,3.s,4.k,2.c,2.k,4.s,.k,3.c,.s,s.c,6.k,2.x,2.k,14.c,6.k,.s,.z,4.k,8.c,.k,9.c,6.k,2.s,3.k,14.c,6.z,d.k,.s,2.k,5i.c,.14,.c,3.14,.c,.14,5k.c,.s,7.k,.c,6.k,.s,.x,2a.c,m.k,.c,.s,7.k,.s,2.k,.s,2.k,3e.c,6.k,3.c,.k,.c,2.k,.c,7.k,.z,.k,1u.c,5.s,.c,2.k,.c,2.s,.k,.s,.k,9n.c,2.k,2.s,9.c,2.k,.z,.s,1c.c,2.s,5.k,3.c,2.s,.k,.10,.k,n.c,.k,3e.c,8.7,4.8,h.7,40e.c,.k,6.c,f.k,8ug.c,c.k,3.s,3.k,1xc.c,5.k,1n.c,7.k,fg.c,.26,3.c,4.26,dg.c,.k,.c,1j.s,7.c,4.k,25.c,2.1v,.1n,.1v,.k,b.c,2.1w,5.1v,9.c,5p2.1v,15.c,w.1v,2p.c,37.1v,6pp.c,4.1v,.c,7.1v,.c,2.1v,.c,83.1v,f.c,.1v,t.c,3.1v,2.c,.1v,e.c,4.1v,8.c,b0.1v,1wh.c,2.k,.c,4.h,318.c,5y.7,q.c,a.b,3.7,3.c,c4.7,6.c,n.7,f.c,h.7,f.c,1a.k,2.c,n.k,f2.c,2.10,3.k,3.c,6.10,8.h,8.k,2.c,7.k,u.c,4.k,1n.c,2.7,l.c,1u.7,3.k,.7,56.c,2f.1n,9.c,n.1v,ne.c,.7,p.c,.7,v.c,.7,p.c,.7,v.c,.7,p.c,.7,v.c,.7,p.c,.7,v.c,.7,p.c,.7,a.c,1e.b,e8.c,1j.k,4.c,1e.k,8.c,.k,e.c,.k,m.c,5.k,.c,f.k,11s.c,7.k,.c,h.k,2.c,7.k,.c,2.k,.c,5.k,2s.c,.k,4g.c,7.k,af.c,.k,1p.c,4.k,f.c,.8,do.c,4.k,72.c,2.k,6r.c,.14,2.c,.14,7.c,2.14,5.c,.14,7e.c,5s.l,7.k,31.l,7.k,md.l,28.n,1s.l,28.n,4w.l,6o.n,2.7,e.n,74.l,4.f,.1m,13.f,4.1r,2s.f,c.1r,f.f,2.1r,f.f,.1r,e.f,.1m,.1r,11.f,a.1r,b.j,2.7,3.f,u.g,.c,.f,1m.g,2.7,4.f,2.1o,c.g,2.1o,e.g,.27,2.g,a.27,i.g,.f,1k.1r,q.28,.1v,2.27,d.1r,a.1v,.27,k.1v,.27,2.1v,9.27,.1v,4.1r,9.1v,7.1r,2.27,e.1r,6.1q,4a.1r,x.1m,.1e,2.f,9.1e,9.1m,.1e,1y.1m,.1e,7.1m,.1t,e.1m,2.f,2.1e,.f,3.1e,2.f,2.1e,y.1m,3.1t,2.1m,.1t,2.1m,.1t,2.1p,2.1e,5.1m,c.1e,h.1m,2.f,.1e,.1m,.1e,.f,.1e,3.1m,5.29,1r.1m,.1e,.1m,.1e,2.1t,2.1m,b.1t,l.1m,j.1t,3.1m,.1t,4.1m,3.1t,.1m,3.1t,7.1m,.1t,.1m,.1t,o.1m,.1t,2a.1m,.1e,.f,1r.1m,8.7,3.f,2.1e,4.1m,.f,o.1m,7.f,2.1e,2.f,.1e,2.1p,4.1e,.1t,c.f,.1e,2.f,4.1e,2.f,.1p,4.f,2.1t,d.f,.1m,.1e,2.f,.1e,8.f,2.1e,9.f,.1e,5.f,3.1e,c.f,3.1e,8.f,3.1e,2.f,.1e,.f,.1e,4.f,.1e,6.f,.1e,3.f,.1e,6.f,.1e,22.1m,3.1t,3.1m,5.1t,1c.7,z.1m,.1t,g.1m,3.1t,9.1m,.1t,5.1m,5.f,.1e,.1t,3.1e,3.1m,2.f,4.1m,3.1r,4.1m,6.1e,3.f,.1e,.f,2.1m,3.1r,.1e,2.f,.1e,9.1m,3.1r,38.7,c.f,2d.7,5.f,6.1r,c.1m,4.1r,.1m,f.1r,c.7,4.1r,1k.7,8.1r,a.7,6.1r,14.7,8.1r,u.7,2.1r,c.f,4.1r,2.f,e.1r,9.f,13.1r,c.7,.1t,2.1m,.1t,8.1m,8.1t,6.1m,.1t,9.1m,a.1t,.1m,.7,3.1t,7.1m,.7,1c.1m,.1t,1p.1m,2.1t,.1m,2.1t,.1m,.1t,h.1m,3.1t,.1m,d.1t,y.1m,2g.f,8.1r,e.f,2.1r,d.1m,3.1r,b.1m,3.1r,1h.1m,3.1t,.1m,.1r,.1m,4.1r,g.1m,2.1r,c.1m,4.1r,.1m,9.1t,7.1r,43.7,.c,2k.7,a.b,.7,5.c,se.1r,2.20,1eke.1v,2.20,1eke.1v,2.20,1eke.c,2.20,1eke.c,2.20,1eke.c,2.20,1eke.c,2.20,1eke.c,2.20,1eke.c,2.20,1eke.c,2.20,1eke.c,2.20,1eke.c,2.20,1eke.c,2.20,w.h,2o.2a,3k.20,6o.k,2s0.20,1bem.c,2.20,1eke.g,2.20,1eke.g,2.20";
const BRACKET_PAIRS = [40,41,91,93,123,125,3898,3899,3900,3901,5787,5788,8261,8262,8317,8318,8333,8334,8968,8969,8970,8971,9001,9002,10088,10089,10090,10091,10092,10093,10094,10095,10096,10097,10098,10099,10100,10101,10181,10182,10214,10215,10216,10217,10218,10219,10220,10221,10222,10223,10627,10628,10629,10630,10631,10632,10633,10634,10635,10636,10637,10640,10639,10638,10641,10642,10643,10644,10645,10646,10647,10648,10712,10713,10714,10715,10748,10749,11810,11811,11812,11813,11814,11815,11816,11817,11861,11862,11863,11864,11865,11866,11867,11868,12296,12297,12298,12299,12300,12301,12302,12303,12304,12305,12308,12309,12310,12311,12312,12313,12314,12315,65113,65114,65115,65116,65117,65118,65288,65289,65339,65341,65371,65373,65375,65376,65378,65379];
const GB_TABLE = "";
// ---- end generated ----

// ------------------------------------------------------------------------------------------------
// Per-code-point properties
// ------------------------------------------------------------------------------------------------
// packed props layout (see build-tables.js):
//   bits 0-2 kind (0 text, 1 hard control, 2 tab, 3 escape introducer)
//   bits 3-7 grapheme break class, bits 8-9 width, bit 10 emoji, bit 11 ambiguous,
//   bits 12-16 bidi class, bit 17 kernel-eligible, bit 18 unsafe follower,
//   bits 20-25 cluster flags (RI, keycap, skin tone, ZWJ, VS16, RTL)
const K_TEXT = 0; // kinds: 0 text, 1 hard control, 2 tab, 3 escape introducer (see build-tables.js)
const P_EMOJI = 1 << 10, P_AMB = 1 << 11, P_KERN = 1 << 17, P_UNSAFE = 1 << 18;
const F_RI = 1 << 20, F_KEYCAP = 1 << 21, F_SKIN = 1 << 22, F_ZWJ = 1 << 23, F_VS16 = 1 << 24, F_RTL = 1 << 25;
const F_MASK = F_RI | F_KEYCAP | F_SKIN | F_ZWJ | F_VS16 | F_RTL;
// Only a cluster's FIRST code point decides the per-cell half of the reorder gate (measured:
// "\u0600\u060D" does not reorder); see hasRtlBlockUnit for the text-level half.
const F_ADD = F_MASK & ~F_RTL;

// grapheme break classes (packed props bits 3-7): 0 Other, 1 Prepend, 2 Regional_Indicator,
// 3 SpacingMark, 4 L, 5 V, 6 T, 7 LV, 8 LVT, 9 ZWJ, 10 Extend, 11 Extended_Pictographic,
// 12 Emoji_Modifier_Base, 13 Emoji_Modifier, 14 InCB=Extend, 15 InCB=Linker, 16 InCB=Consonant
const G_OTHER = 0;
const NG = 17;

const IDS = new Uint8Array(0x110000);
const PROP = Int32Array.from(PROPS);
const P16 = new Int32Array(0x10000); // props of BMP code points, one lookup in the hot loops
{
  // ID_RLE: comma-separated runs "len.id" (base 36; len omitted when 1)
  let cp = 0, num = 0, len = 1;
  for (let k = 0, L = ID_RLE.length; k <= L; k++) {
    const ch = k < L ? ID_RLE.charCodeAt(k) : 44;
    if (ch === 46) { len = num === 0 ? 1 : num; num = 0; }
    else if (ch === 44) {
      IDS.fill(num, cp, cp + len);
      if (cp < 0x10000) P16.fill(PROP[num], cp, Math.min(cp + len, 0x10000));
      cp += len; num = 0; len = 1;
    } else num = num * 36 + (ch <= 57 ? ch - 48 : ch - 87);
  }
  if (cp !== 0x110000) throw new Error("cell-segmenter: corrupt table");
}
const propOf = (cp) => (cp < 0x10000 ? P16[cp] : PROP[IDS[cp]]);

// ------------------------------------------------------------------------------------------------
// Grapheme break state machine (uucode computeGraphemeBreakNoControl, as used by the native)
// GB[(state * NG + g1) * NG + g2] = (newState << 1) | break
// ------------------------------------------------------------------------------------------------
const S_DEFAULT = 0; // break states: 0 Default, 1 RI, 2 EP, 3 ICB_CONS, 4 ICB_LINK
// GB_TABLE is generated (build-tables.js) from the break rules: in UAX #29 terms GB6-GB8 (Hangul),
// GB9a/GB9b, GB9c (Indic conjuncts, Unicode 16 InCB data), GB11 plus emoji modifier sequences
// (a modifier joins only directly after an Emoji_Modifier_Base), GB12/GB13 and GB9. The state
// carries across a break and is reset after a hard break.
const GB = new Uint8Array(5 * NG * NG);
for (let k = 0; k < GB.length; k++) GB[k] = GB_TABLE.charCodeAt(k) - 48;

// ------------------------------------------------------------------------------------------------
// Errors (Node-style: name TypeError/RangeError, own `code`, String(e) shows the code)
// ------------------------------------------------------------------------------------------------
function nodeError(Ctor, code, message) {
  const e = new Ctor(message);
  Object.defineProperty(e, "code", { value: code, writable: true, enumerable: true, configurable: true });
  Object.defineProperty(e, "toString", {
    value() { return `${this.name} [${code}]: ${this.message}`; }, writable: true, enumerable: false, configurable: true,
  });
  return e;
}
const argTypeError = (msg) => nodeError(TypeError, "ERR_INVALID_ARG_TYPE", msg);
const rangeError = (msg) => nodeError(RangeError, "ERR_OUT_OF_RANGE", msg);
const invalidThis = (self) => nodeError(TypeError, "ERR_INVALID_THIS", "Expected this to be instanceof CellSegmenter" +
  (self !== null && typeof self === "object" ? `, but received an instance of ${ctorName(self)}` : ""));
function ctorName(o) {
  try { const c = o.constructor; if (typeof c === "function" && c.name) return c.name; } catch { /* ignore */ }
  return "Object";
}
const HAS_SAB = typeof SharedArrayBuffer === "function";
function isI32(v) {
  if (!(v instanceof Int32Array)) return false;
  const b = v.buffer;
  if (HAS_SAB && b instanceof SharedArrayBuffer) return false;
  if (b.resizable === true || b.growable === true) return false;
  return true;
}
const i32Error = (name) => argTypeError(`Expected ${name} to be a non-shared, fixed-length, 4-byte-aligned Int32Array for 'CellSegmenter'.`);
function describeNumber(v) { return String(v); }
// screenWidth / count: undefined and NaN are 0; number, integer, 0 <= v <= 2^32-1.
function toU32Arg(v, name) {
  if (v === undefined) return 0;
  if (typeof v !== "number") throw argTypeError(`The "${name}" property must be of type number. Received ${typeof v}`);
  if (v !== v) return 0;
  if (v === Infinity || v === -Infinity || (Number.isInteger(v) && (v < 0 || v > 4294967295)))
    throw rangeError(`The value of "${name}" is out of range. It must be >= 0 and <= 9007199254740991. Received ${describeNumber(v)}`);
  if (!Number.isInteger(v)) throw argTypeError(`The "${name}" property must be of type integer. Received number`);
  return v === 0 ? 0 : v;
}
// constructor integer fields (screen.*): number, integer, int32 range
function toI32Field(v, name, dflt) {
  if (v === undefined) return dflt;
  if (typeof v !== "number") throw argTypeError(`The "${name}" property must be of type number. Received ${typeof v}`);
  if (v !== v || v === Infinity || v === -Infinity || (Number.isInteger(v) && (v < -2147483648 || v > 2147483647)))
    throw rangeError(`The value of "${name}" is out of range. It must be >= -9007199254740991 and <= 9007199254740991. Received ${describeNumber(v)}`);
  if (!Number.isInteger(v)) throw argTypeError(`The "${name}" property must be of type integer. Received number`);
  return v | 0;
}
// ToInt32 as the native binding does it (measured): a BigInt primitive wraps modulo 2^32, anything
// else goes through ToNumber (so a BigInt from valueOf/toPrimitive throws). Engine TypeErrors are
// reworded to JavaScriptCore's text so Node reports the same messages as Bun.
function toInt32Slow(v) {
  if (typeof v === "bigint") return Number(BigInt.asIntN(32, v));
  if (typeof v === "symbol") throw new TypeError("Cannot convert a symbol to a number");
  try { return +v | 0; } catch (e) { throw jscTypeError(e); }
}
function jscTypeError(e) {
  if (e instanceof TypeError) {
    if (e.message === "Cannot convert a Symbol value to a number") return new TypeError("Cannot convert a symbol to a number");
    if (e.message === "Cannot convert a BigInt value to a number") return new TypeError("Conversion from 'BigInt' to 'number' is not allowed.");
  }
  return e;
}
const toInt32 = (v) => (typeof v === "number" ? v | 0 : toInt32Slow(v));

// ------------------------------------------------------------------------------------------------
// SGR model
// ------------------------------------------------------------------------------------------------
// slot ids; 18 = the shared "unknown" slot (close ESC[0m)
const SLOT_UNKNOWN = 18;
const SLOT = new Uint8Array(256).fill(SLOT_UNKNOWN), END_OF = new Uint8Array(256);
{
  const put = (codes, slot, end) => { for (const c of codes) { SLOT[c] = slot; END_OF[c] = end; } };
  put([1], 1, 22); put([2], 2, 22); put([3], 3, 23); put([20], 4, 23); put([4], 5, 24); put([21], 6, 24);
  put([5, 6], 7, 25); put([7], 8, 27); put([8], 9, 28); put([9], 10, 29);
  put([30, 31, 32, 33, 34, 35, 36, 37, 38, 90, 91, 92, 93, 94, 95, 96, 97], 11, 39);
  put([40, 41, 42, 43, 44, 45, 46, 47, 48, 100, 101, 102, 103, 104, 105, 106, 107], 12, 49);
  put([51], 13, 54); put([52], 14, 54); put([53], 15, 55); put([58], 16, 59); put([73, 74], 17, 75);
}
const IS_END = new Uint8Array(256);
for (const e of [22, 23, 24, 25, 27, 28, 29, 39, 49, 54, 55, 59, 75]) IS_END[e] = 1;
// entry: {s, slot, end, opaque, open, close}
function makeEntry(s, slot, end, opaque) {
  return { s, slot, end, opaque, open: "\x1b[" + s + "m", close: "\x1b[" + end + "m" };
}
const SIMPLE = [];
for (let c = 0; c < 256; c++) SIMPLE.push(makeEntry(String(c), SLOT[c], END_OF[c], false));
const FIELDS = new Float64Array(33);

// Canonical entry objects for extended colours and opaque sequences (so the key trie can use
// object identity); bounded caches.
const ENTRY_CACHE = new Map();
function entryFor(s, slot, end, opaque) {
  const k = opaque ? "\u0001" + s : s;
  let e = ENTRY_CACHE.get(k);
  if (e === undefined) {
    if (ENTRY_CACHE.size >= 8192) ENTRY_CACHE.clear();
    e = makeEntry(s, slot, end, opaque);
    ENTRY_CACHE.set(k, e);
  }
  return e;
}
// Parse an SGR parameter string into ops: 0 = reset, an end code (number) = remove by close code,
// an entry = start code (or ONE opaque entry for the whole sequence).
function parseSgr(str) {
  let nf = 0, v = 0, colon = false, big = false;
  for (let j = 0; j < str.length; j++) {
    const c = str.charCodeAt(j);
    if (c === 0x3b) { if (nf < 33) FIELDS[nf] = v; nf++; v = 0; }
    else if (c === 0x3a) colon = true;
    else if (!colon) { v = v * 10 + (c - 48); if (v > 255) { big = true; v = 256; } }
  }
  if (nf < 33) FIELDS[nf] = v;
  nf++;
  if (colon || nf > 32 || big) {
    // opaque: ONE raw entry, slotted by the leading digits of the first field
    let f = 0, any = false;
    for (let j = 0; j < str.length; j++) { const c = str.charCodeAt(j); if (c < 0x30 || c > 0x39) break; any = true; f = f * 10 + (c - 48); if (f > 255) f = 256; }
    let slot = SLOT_UNKNOWN, end = 0;
    if (any && f > 0 && f <= 255 && !IS_END[f] && SLOT[f] !== SLOT_UNKNOWN) { slot = SLOT[f]; end = END_OF[f]; }
    return [entryFor(str, slot, end, true)];
  }
  const ops = [];
  for (let k = 0; k < nf; k++) {
    const code = FIELDS[k];
    if ((code === 38 || code === 48 || code === 58) && k + 1 < nf && FIELDS[k + 1] === 5 && k + 2 < nf) {
      ops.push(entryFor(code + ";5;" + FIELDS[k + 2], SLOT[code], END_OF[code], false));
      k += 2;
    } else if ((code === 38 || code === 48 || code === 58) && k + 1 < nf && FIELDS[k + 1] === 2 && k + 4 < nf) {
      ops.push(entryFor(code + ";2;" + FIELDS[k + 2] + ";" + FIELDS[k + 3] + ";" + FIELDS[k + 4], SLOT[code], END_OF[code], false));
      k += 4;
    } else if (code === 0 || IS_END[code]) ops.push(code);
    else ops.push(SIMPLE[code]);
  }
  return ops;
}
const SGR_CACHE_SIZE = 1024;
const SGR_CACHE_KEYS = new Array(SGR_CACHE_SIZE).fill(null), SGR_CACHE_OPS = new Array(SGR_CACHE_SIZE).fill(null);
function applyOp(list, op) {
  if (typeof op === "number") {
    if (op === 0) { if (list.length !== 0) list.length = 0; return; }
    let w = 0;
    for (let r = 0; r < list.length; r++) if (list[r].end !== op) list[w++] = list[r];
    if (w !== list.length) list.length = w;
    return;
  }
  if (!op.opaque) {
    for (let r = 0; r < list.length; r++) if (!list[r].opaque && list[r].s === op.s) return; // identical: no-op
  }
  removeSlot(list, op.slot);
  list.push(op);
}
// Apply a valid SGR parameter string text[a, b) (bytes 0-9 : ; only) to list (array of entries).
function applySgr(list, text, a, b) {
  if (b - a <= 3) {
    // fast path: "", "N", "NN", "NNN" (a single simple code)
    let v = 0, j = a;
    for (; j < b; j++) { const c = text.charCodeAt(j); if (c > 0x39) break; v = v * 10 + (c - 48); }
    if (j === b && v <= 255) { applyOp(list, v === 0 || IS_END[v] ? v : SIMPLE[v]); return; }
  }
  // direct-mapped cache of parsed parameter strings (no allocation on a hit)
  let h = 0x811c9dc5;
  for (let j = a; j < b; j++) h = Math.imul(h ^ text.charCodeAt(j), 16777619);
  const slot = (h >>> 0) & (SGR_CACHE_SIZE - 1);
  const k0 = SGR_CACHE_KEYS[slot];
  let ops;
  if (k0 !== null && k0.length === b - a && sameChars(k0, text, a)) ops = SGR_CACHE_OPS[slot];
  else {
    const key = text.slice(a, b);
    ops = parseSgr(key);
    SGR_CACHE_KEYS[slot] = key; SGR_CACHE_OPS[slot] = ops;
  }
  for (let k = 0; k < ops.length; k++) applyOp(list, ops[k]);
}
function sameChars(key, text, a) {
  for (let q = 0; q < key.length; q++) if (key.charCodeAt(q) !== text.charCodeAt(a + q)) return false;
  return true;
}
function removeSlot(list, slot) {
  let w = 0;
  for (let r = 0; r < list.length; r++) if (list[r].slot !== slot) list[w++] = list[r];
  if (w !== list.length) list.length = w;
}

// ------------------------------------------------------------------------------------------------
// Bidi levels: a port of ICU 78.3 ubidi.cpp (ubidi_setPara with paraLevel UBIDI_DEFAULT_LTR, the
// default reordering mode, no options, no prologue/epilogue). The native segmenter runs ICU on the
// UTF-16 bidi text, so this port works on UTF-16 code units exactly like ICU: the lead surrogate of
// a supplementary character is BN. ICU quirks are kept on purpose (for example, without explicit
// codes the paired-bracket state at a paragraph separator is reset with the level of the paragraph
// that ends, not of the one that starts).
// ------------------------------------------------------------------------------------------------
const D_L = 0, D_R = 1, D_EN = 2, D_ES = 3, D_ET = 4, D_AN = 5, D_CS = 6, D_B = 7, D_S = 8, D_WS = 9, D_ON = 10,
  D_LRE = 11, D_LRO = 12, D_AL = 13, D_RLE = 14, D_RLO = 15, D_PDF = 16, D_NSM = 17, D_BN = 18, D_FSI = 19,
  D_LRI = 20, D_RLI = 21, D_PDI = 22, D_ENL = 23, D_ENR = 24;
// bidi class index in the packed props (L R AL EN ES ET AN CS NSM BN B S WS ON LRE LRO RLE RLO PDF LRI RLI FSI PDI)
const TO_ICU = Uint8Array.from([D_L, D_R, D_AL, D_EN, D_ES, D_ET, D_AN, D_CS, D_NSM, D_BN, D_B, D_S, D_WS, D_ON,
  D_LRE, D_LRO, D_RLE, D_RLO, D_PDF, D_LRI, D_RLI, D_FSI, D_PDI]);
const DF = (d) => 1 << d;
const MASK_LTR = DF(D_L) | DF(D_EN) | DF(D_ENL) | DF(D_ENR) | DF(D_AN) | DF(D_LRE) | DF(D_LRO) | DF(D_LRI);
const MASK_RTL = DF(D_R) | DF(D_AL) | DF(D_RLE) | DF(D_RLO) | DF(D_RLI);
const MASK_EXPLICIT = DF(D_LRE) | DF(D_LRO) | DF(D_RLE) | DF(D_RLO) | DF(D_PDF);
const MASK_ISO = DF(D_LRI) | DF(D_RLI) | DF(D_FSI) | DF(D_PDI);
const MASK_BN_EXPLICIT = DF(D_BN) | MASK_EXPLICIT;
const MASK_B_S = DF(D_B) | DF(D_S);
const MASK_WS = MASK_B_S | DF(D_WS) | MASK_BN_EXPLICIT | MASK_ISO;
const MASK_POSSIBLE_N = DF(D_ON) | DF(D_CS) | DF(D_ES) | DF(D_ET) | MASK_WS;
const MASK_EMBEDDING = DF(D_NSM) | MASK_POSSIBLE_N;
const FLAG_LR = [DF(D_L), DF(D_R)], FLAG_E = [DF(D_LRE), DF(D_RLE)], FLAG_O = [DF(D_LRO), DF(D_RLO)];
const LEVEL_OVERRIDE = 0x80, MAX_EXPLICIT_LEVEL = 125, ISOLATE = 0x100;
const NO_OVERRIDE = (l) => l & ~LEVEL_OVERRIDE;
const DIR_MIXED = 2;

// paired brackets (BidiBrackets.txt): code unit -> paired code unit; type 1 open, 2 close
const BPB = new Map(), BPT = new Map();
for (let i = 0; i < BRACKET_PAIRS.length; i += 2) {
  const o = BRACKET_PAIRS[i], c = BRACKET_PAIRS[i + 1];
  BPB.set(o, c); BPB.set(c, o); BPT.set(o, 1); BPT.set(c, 2);
}

function bidiClassOfUnitCp(cp) { return TO_ICU[(propOf(cp) >> 12) & 31]; }

// text: Uint16Array of UTF-16 units (length n > 0). Returns levels (Uint8Array, per unit).
function icuLevels(text, n) {
  const B = {
    text, n, dirProps: new Uint8Array(n), levels: new Uint8Array(n), paras: [{ limit: n, level: 0 }],
    paraLevel: 0, flags: 0, multiRuns: false, isolateCount: 0, isolates: [],
  };
  getDirProps(B);
  let direction = resolveExplicitLevels(B);
  const levels = B.levels;
  if (direction !== DIR_MIXED) {
    levels.fill(B.paraLevel); // all levels are implicitly at paraLevel
    return levels;
  }
  B.isolateCount = -1;
  const dirProps = B.dirProps;
  if (B.paras.length <= 1 && !B.multiRuns) {
    resolveImplicitLevels(B, 0, n, paraLevelAt(B, 0) & 1, paraLevelAt(B, n - 1) & 1);
  } else {
    let start, limit = 0, level, nextLevel, sor, eor;
    level = paraLevelAt(B, 0);
    nextLevel = levels[0];
    eor = level < nextLevel ? nextLevel & 1 : level & 1;
    do {
      start = limit;
      level = nextLevel;
      if (start > 0 && dirProps[start - 1] === D_B) sor = paraLevelAt(B, start) & 1;
      else sor = eor;
      while (++limit < n && (levels[limit] === level || (DF(dirProps[limit]) & MASK_BN_EXPLICIT))) { /* scan */ }
      nextLevel = limit < n ? levels[limit] : paraLevelAt(B, n - 1);
      eor = NO_OVERRIDE(level) < NO_OVERRIDE(nextLevel) ? nextLevel & 1 : level & 1;
      if (!(level & LEVEL_OVERRIDE)) resolveImplicitLevels(B, start, limit, sor, eor);
      else { do { levels[start++] &= ~LEVEL_OVERRIDE; } while (start < limit); }
    } while (limit < n);
  }
  adjustWSLevels(B);
  return levels;
}

function paraLevelAt(B, index) {
  if (index < B.paras[0].limit) return B.paraLevel;
  let i = 0;
  for (; i < B.paras.length; i++) if (index < B.paras[i].limit) break;
  if (i >= B.paras.length) i = B.paras.length - 1;
  return B.paras[i].level;
}

const GDP_ISO = new Int32Array(MAX_EXPLICIT_LEVEL + 1), GDP_PREV = new Int32Array(MAX_EXPLICIT_LEVEL + 1);
function getDirProps(B) {
  const text = B.text, n = B.n, dirProps = B.dirProps;
  const NOT_SEEKING_STRONG = 0, SEEKING_STRONG_FOR_PARA = 1, SEEKING_STRONG_FOR_FSI = 2, LOOKING_FOR_PDI = 3;
  let flags = 0, i = 0, state = SEEKING_STRONG_FOR_PARA, stackLast = -1;
  const defaultParaLevel = 0; // UBIDI_DEFAULT_LTR & 1
  const isolateStartStack = GDP_ISO, previousStateStack = GDP_PREV; // scratch (fully rewritten before read)
  const paras = B.paras;
  paras[0].level = defaultParaLevel;
  while (i < n) {
    let uchar = text[i++];
    if ((uchar & 0xfc00) === 0xd800 && i < n && (text[i] & 0xfc00) === 0xdc00) uchar = 0x10000 + ((uchar - 0xd800) << 10) + (text[i++] - 0xdc00);
    const dirProp = bidiClassOfUnitCp(uchar);
    flags |= DF(dirProp);
    dirProps[i - 1] = dirProp;
    if (uchar > 0xffff) { flags |= DF(D_BN); dirProps[i - 2] = D_BN; }
    if (dirProp === D_L) {
      if (state === SEEKING_STRONG_FOR_PARA) { paras[paras.length - 1].level = 0; state = NOT_SEEKING_STRONG; }
      else if (state === SEEKING_STRONG_FOR_FSI) { if (stackLast <= MAX_EXPLICIT_LEVEL) flags |= DF(D_LRI); state = LOOKING_FOR_PDI; }
      continue;
    }
    if (dirProp === D_R || dirProp === D_AL) {
      if (state === SEEKING_STRONG_FOR_PARA) { paras[paras.length - 1].level = 1; state = NOT_SEEKING_STRONG; }
      else if (state === SEEKING_STRONG_FOR_FSI) {
        if (stackLast <= MAX_EXPLICIT_LEVEL) { dirProps[isolateStartStack[stackLast]] = D_RLI; flags |= DF(D_RLI); }
        state = LOOKING_FOR_PDI;
      }
      continue;
    }
    if (dirProp >= D_FSI && dirProp <= D_RLI) {
      stackLast++;
      if (stackLast <= MAX_EXPLICIT_LEVEL) { isolateStartStack[stackLast] = i - 1; previousStateStack[stackLast] = state; }
      if (dirProp === D_FSI) { dirProps[i - 1] = D_LRI; state = SEEKING_STRONG_FOR_FSI; }
      else state = LOOKING_FOR_PDI;
      continue;
    }
    if (dirProp === D_PDI) {
      if (state === SEEKING_STRONG_FOR_FSI) { if (stackLast <= MAX_EXPLICIT_LEVEL) flags |= DF(D_LRI); }
      if (stackLast >= 0) {
        if (stackLast <= MAX_EXPLICIT_LEVEL) state = previousStateStack[stackLast];
        stackLast--;
      }
      continue;
    }
    if (dirProp === D_B) {
      if (i < n && uchar === 0x0d && text[i] === 0x0a) continue;
      paras[paras.length - 1].limit = i;
      if (i < n) {
        paras.push({ limit: n, level: defaultParaLevel });
        state = SEEKING_STRONG_FOR_PARA;
        stackLast = -1;
      }
      continue;
    }
  }
  if (stackLast > MAX_EXPLICIT_LEVEL) { stackLast = MAX_EXPLICIT_LEVEL; state = SEEKING_STRONG_FOR_FSI; }
  while (stackLast >= 0) {
    if (state === SEEKING_STRONG_FOR_FSI) { flags |= DF(D_LRI); break; }
    state = previousStateStack[stackLast];
    stackLast--;
  }
  paras[paras.length - 1].limit = n;
  B.paraLevel = paras[0].level;
  for (let k = 0; k < paras.length; k++) flags |= FLAG_LR[paras[k].level & 1];
  B.flags = flags;
}

function directionFromFlags(flags) {
  if (!((flags & MASK_RTL) || ((flags & DF(D_AN)) && (flags & MASK_POSSIBLE_N)))) return 0; // LTR
  if (!(flags & MASK_LTR)) return 1; // RTL
  return DIR_MIXED;
}

// ---- paired brackets (N0), processed inline with the explicit levels, as ICU does
function bracketInit(B) {
  const t = paraLevelAt(B, 0) & 1;
  return {
    B, isoRunLast: 0, openings: [],
    isoRuns: [{ start: 0, limit: 0, level: paraLevelAt(B, 0), lastStrong: t, lastBase: t, contextDir: t, contextPos: 0 }],
  };
}
function isoRun(bd, k) {
  let r = bd.isoRuns[k];
  if (!r) bd.isoRuns[k] = r = { start: 0, limit: 0, level: 0, lastStrong: 0, lastBase: 0, contextDir: 0, contextPos: 0 };
  return r;
}
function bracketProcessB(bd, level) {
  bd.isoRunLast = 0;
  const r = bd.isoRuns[0];
  r.limit = 0; r.level = level; r.lastStrong = r.lastBase = level & 1; r.contextDir = level & 1; r.contextPos = 0;
}
function bracketProcessBoundary(bd, lastCcPos, contextLevel, embeddingLevel) {
  const r = bd.isoRuns[bd.isoRunLast];
  if (DF(bd.B.dirProps[lastCcPos]) & MASK_ISO) return;
  if (NO_OVERRIDE(embeddingLevel) > NO_OVERRIDE(contextLevel)) contextLevel = embeddingLevel;
  r.limit = r.start; r.level = embeddingLevel; r.lastStrong = r.lastBase = contextLevel & 1;
  r.contextDir = contextLevel & 1; r.contextPos = lastCcPos;
}
function bracketProcessLRI_RLI(bd, level) {
  const last = bd.isoRuns[bd.isoRunLast];
  last.lastBase = D_ON;
  const lastLimit = last.limit;
  bd.isoRunLast++;
  const r = isoRun(bd, bd.isoRunLast);
  r.start = r.limit = lastLimit; r.level = level; r.lastStrong = r.lastBase = level & 1; r.contextDir = level & 1; r.contextPos = 0;
}
function bracketProcessPDI(bd) {
  bd.isoRunLast--;
  bd.isoRuns[bd.isoRunLast].lastBase = D_ON;
}
function bracketAddOpening(bd, match, position) {
  const r = bd.isoRuns[bd.isoRunLast];
  bd.openings[r.limit] = { position, match, contextDir: r.contextDir, contextPos: r.contextPos, flags: 0 };
  r.limit++;
}
function fixN0c(bd, openingIndex, newPropPosition, newProp) {
  const r = bd.isoRuns[bd.isoRunLast], dirProps = bd.B.dirProps;
  for (let k = openingIndex + 1; k < r.limit; k++) {
    const q = bd.openings[k];
    if (q.match >= 0) continue;
    if (newPropPosition < q.contextPos) break;
    if (newPropPosition >= q.position) continue;
    if (newProp === q.contextDir) break;
    const openingPosition = q.position;
    dirProps[openingPosition] = newProp;
    const closingPosition = -q.match;
    dirProps[closingPosition] = newProp;
    q.match = 0;
    fixN0c(bd, k, openingPosition, newProp);
    fixN0c(bd, k, closingPosition, newProp);
  }
}
function bracketProcessClosing(bd, openIdx, position) {
  const r = bd.isoRuns[bd.isoRunLast];
  const o = bd.openings[openIdx];
  const direction = r.level & 1;
  let stable = true, newProp;
  if ((direction === 0 && (o.flags & DF(D_L))) || (direction === 1 && (o.flags & DF(D_R)))) newProp = direction; // N0b
  else if (o.flags & (DF(D_L) | DF(D_R))) { // N0c
    stable = openIdx === r.start;
    newProp = direction !== o.contextDir ? o.contextDir : direction;
  } else { r.limit = openIdx; return D_ON; } // N0d
  bd.B.dirProps[o.position] = newProp;
  bd.B.dirProps[position] = newProp;
  fixN0c(bd, openIdx, o.position, newProp);
  if (stable) {
    r.limit = openIdx;
    while (r.limit > r.start && bd.openings[r.limit - 1].position === o.position) r.limit--;
  } else {
    o.match = -position;
    let k = openIdx - 1;
    while (k >= r.start && bd.openings[k].position === o.position) bd.openings[k--].match = 0;
    for (k = openIdx + 1; k < r.limit; k++) {
      const q = bd.openings[k];
      if (q.position >= position) break;
      if (q.match > 0) q.match = 0;
    }
  }
  return newProp;
}
function bracketProcessChar(bd, position) {
  const B = bd.B, dirProps = B.dirProps, r = bd.isoRuns[bd.isoRunLast];
  const dirProp = dirProps[position];
  let newProp, level;
  if (dirProp === D_ON) {
    let c = B.text[position];
    for (let idx = r.limit - 1; idx >= r.start; idx--) {
      if (bd.openings[idx].match !== c) continue;
      newProp = bracketProcessClosing(bd, idx, position);
      if (newProp === D_ON) { c = 0; break; } // N0d
      r.lastBase = D_ON;
      r.contextDir = newProp;
      r.contextPos = position;
      level = B.levels[position];
      if (level & LEVEL_OVERRIDE) {
        newProp = level & 1;
        r.lastStrong = newProp;
        const flag = DF(newProp);
        for (let i = r.start; i < idx; i++) bd.openings[i].flags |= flag;
        B.levels[position] &= ~LEVEL_OVERRIDE;
      }
      B.levels[bd.openings[idx].position] &= ~LEVEL_OVERRIDE;
      return;
    }
    const match = c ? (BPB.get(c) ?? c) : 0;
    if (match !== c && BPT.get(c) === 1) {
      if (match === 0x232a) bracketAddOpening(bd, 0x3009, position);
      else if (match === 0x3009) bracketAddOpening(bd, 0x232a, position);
      bracketAddOpening(bd, match, position);
    }
  }
  level = B.levels[position];
  if (level & LEVEL_OVERRIDE) {
    newProp = level & 1;
    if (dirProp !== D_S && dirProp !== D_WS && dirProp !== D_ON) dirProps[position] = newProp;
    r.lastBase = newProp; r.lastStrong = newProp; r.contextDir = newProp; r.contextPos = position;
  } else if (dirProp <= D_R || dirProp === D_AL) {
    newProp = dirProp === D_L ? D_L : D_R;
    r.lastBase = dirProp; r.lastStrong = dirProp; r.contextDir = newProp; r.contextPos = position;
  } else if (dirProp === D_EN) {
    r.lastBase = D_EN;
    if (r.lastStrong === D_L) {
      newProp = D_L;
      dirProps[position] = D_ENL;
      r.contextDir = D_L; r.contextPos = position;
    } else {
      newProp = D_R;
      dirProps[position] = r.lastStrong === D_AL ? D_AN : D_ENR;
      r.contextDir = D_R; r.contextPos = position;
    }
  } else if (dirProp === D_AN) {
    newProp = D_R;
    r.lastBase = D_AN; r.contextDir = D_R; r.contextPos = position;
  } else if (dirProp === D_NSM) {
    newProp = r.lastBase;
    if (newProp === D_ON) dirProps[position] = newProp;
  } else {
    newProp = dirProp;
    r.lastBase = dirProp;
  }
  if (newProp <= D_R || newProp === D_AL) {
    const flag = DF(newProp === D_L ? D_L : D_R);
    for (let i = r.start; i < r.limit; i++) if (position > bd.openings[i].position) bd.openings[i].flags |= flag;
  }
}

function resolveExplicitLevels(B) {
  const dirProps = B.dirProps, levels = B.levels, text = B.text, n = B.n;
  let flags = B.flags;
  let level = paraLevelAt(B, 0);
  B.isolateCount = 0;
  let direction = directionFromFlags(flags);
  if (direction !== DIR_MIXED) return direction;
  if (!(flags & (MASK_EXPLICIT | MASK_ISO))) {
    const bd = bracketInit(B);
    for (let pi = 0; pi < B.paras.length; pi++) {
      const start = pi === 0 ? 0 : B.paras[pi - 1].limit, limit = B.paras[pi].limit;
      level = B.paras[pi].level;
      for (let i = start; i < limit; i++) {
        levels[i] = level;
        const dirProp = dirProps[i];
        if (dirProp === D_BN) continue;
        if (dirProp === D_B) {
          if (i + 1 < n) {
            if (text[i] === 0x0d && text[i + 1] === 0x0a) continue;
            bracketProcessB(bd, level);
          }
          continue;
        }
        bracketProcessChar(bd, i);
      }
    }
    return direction;
  }
  let embeddingLevel = level, newLevel, previousLevel = level, lastCcPos = 0;
  const stack = new Uint16Array(MAX_EXPLICIT_LEVEL + 2);
  let stackLast = 0, overflowIsolateCount = 0, overflowEmbeddingCount = 0, validIsolateCount = 0;
  const bd = bracketInit(B);
  stack[0] = level;
  flags = 0;
  for (let i = 0; i < n; ++i) {
    const dirProp = dirProps[i];
    switch (dirProp) {
      case D_LRE: case D_RLE: case D_LRO: case D_RLO:
        flags |= DF(D_BN);
        levels[i] = previousLevel;
        if (dirProp === D_LRE || dirProp === D_LRO) newLevel = (embeddingLevel + 2) & ~(LEVEL_OVERRIDE | 1);
        else newLevel = (NO_OVERRIDE(embeddingLevel) + 1) | 1;
        if (newLevel <= MAX_EXPLICIT_LEVEL && overflowIsolateCount === 0 && overflowEmbeddingCount === 0) {
          lastCcPos = i;
          embeddingLevel = newLevel;
          if (dirProp === D_LRO || dirProp === D_RLO) embeddingLevel |= LEVEL_OVERRIDE;
          stackLast++;
          stack[stackLast] = embeddingLevel;
        } else if (overflowIsolateCount === 0) overflowEmbeddingCount++;
        break;
      case D_PDF:
        flags |= DF(D_BN);
        levels[i] = previousLevel;
        if (overflowIsolateCount) break;
        if (overflowEmbeddingCount) { overflowEmbeddingCount--; break; }
        // (UBiDiLevel) cast in ICU: an isolate entry below loses its ISOLATE flag (0x100)
        if (stackLast > 0 && stack[stackLast] < ISOLATE) { lastCcPos = i; stackLast--; embeddingLevel = stack[stackLast] & 0xff; }
        break;
      case D_LRI: case D_RLI:
        flags |= DF(D_ON) | FLAG_LR[embeddingLevel & 1];
        levels[i] = NO_OVERRIDE(embeddingLevel);
        if (NO_OVERRIDE(embeddingLevel) !== NO_OVERRIDE(previousLevel)) {
          bracketProcessBoundary(bd, lastCcPos, previousLevel, embeddingLevel);
          B.multiRuns = true;
        }
        previousLevel = embeddingLevel;
        if (dirProp === D_LRI) newLevel = (embeddingLevel + 2) & ~(LEVEL_OVERRIDE | 1);
        else newLevel = (NO_OVERRIDE(embeddingLevel) + 1) | 1;
        if (newLevel <= MAX_EXPLICIT_LEVEL && overflowIsolateCount === 0 && overflowEmbeddingCount === 0) {
          flags |= DF(dirProp);
          lastCcPos = i;
          validIsolateCount++;
          if (validIsolateCount > B.isolateCount) B.isolateCount = validIsolateCount;
          embeddingLevel = newLevel;
          stackLast++;
          stack[stackLast] = embeddingLevel + ISOLATE;
          bracketProcessLRI_RLI(bd, embeddingLevel);
        } else {
          dirProps[i] = D_WS;
          overflowIsolateCount++;
        }
        break;
      case D_PDI:
        if (NO_OVERRIDE(embeddingLevel) !== NO_OVERRIDE(previousLevel)) {
          bracketProcessBoundary(bd, lastCcPos, previousLevel, embeddingLevel);
          B.multiRuns = true;
        }
        if (overflowIsolateCount) { overflowIsolateCount--; dirProps[i] = D_WS; }
        else if (validIsolateCount) {
          flags |= DF(D_PDI);
          lastCcPos = i;
          overflowEmbeddingCount = 0;
          while (stack[stackLast] < ISOLATE) stackLast--;
          stackLast--;
          validIsolateCount--;
          bracketProcessPDI(bd);
        } else dirProps[i] = D_WS;
        embeddingLevel = stack[stackLast] & 0xff;
        flags |= DF(D_ON) | FLAG_LR[embeddingLevel & 1];
        previousLevel = embeddingLevel;
        levels[i] = NO_OVERRIDE(embeddingLevel);
        break;
      case D_B:
        flags |= DF(D_B);
        levels[i] = paraLevelAt(B, i);
        if (i + 1 < n) {
          if (text[i] === 0x0d && text[i + 1] === 0x0a) break;
          overflowEmbeddingCount = overflowIsolateCount = 0;
          validIsolateCount = 0;
          stackLast = 0;
          previousLevel = embeddingLevel = paraLevelAt(B, i + 1);
          stack[0] = embeddingLevel;
          bracketProcessB(bd, embeddingLevel);
        }
        break;
      case D_BN:
        levels[i] = previousLevel;
        flags |= DF(D_BN);
        break;
      default:
        if (NO_OVERRIDE(embeddingLevel) !== NO_OVERRIDE(previousLevel)) {
          bracketProcessBoundary(bd, lastCcPos, previousLevel, embeddingLevel);
          B.multiRuns = true;
          if (embeddingLevel & LEVEL_OVERRIDE) flags |= FLAG_O[embeddingLevel & 1];
          else flags |= FLAG_E[embeddingLevel & 1];
        }
        previousLevel = embeddingLevel;
        levels[i] = embeddingLevel;
        bracketProcessChar(bd, i);
        flags |= DF(dirProps[i]);
        break;
    }
  }
  if (flags & MASK_EMBEDDING) flags |= FLAG_LR[B.paraLevel & 1];
  B.flags = flags;
  return directionFromFlags(flags);
}

// ---- implicit levels (Wn, Nn, In) via ICU's state tables
const GROUP_PROP = Uint8Array.from([0, 1, 2, 7, 8, 3, 9, 6, 5, 4, 4, 10, 10, 12, 10, 10, 10, 11, 10, 4, 4, 4, 4, 13, 14]);
const DP_L = 0, DP_R = 1, DP_EN = 2, DP_AN = 3, DP_ON = 4, DP_S = 5, DP_B = 6;
const sp = (a, s) => s + (a << 5);
const IMP_TAB_PROPS = [
  /*                L ,     R ,     EN ,    AN ,    ON ,     S ,     B ,    ES ,    ET ,    CS ,    BN ,   NSM ,    AL ,   ENL ,   ENR , Res */
  /* 0 Init */     [1, 2, 4, 5, 7, 15, 17, 7, 9, 7, 0, 7, 3, 18, 21, DP_ON],
  /* 1 L */        [1, sp(1, 2), sp(1, 4), sp(1, 5), sp(1, 7), sp(1, 15), sp(1, 17), sp(1, 7), sp(1, 9), sp(1, 7), 1, 1, sp(1, 3), sp(1, 18), sp(1, 21), DP_L],
  /* 2 R */        [sp(1, 1), 2, sp(1, 4), sp(1, 5), sp(1, 7), sp(1, 15), sp(1, 17), sp(1, 7), sp(1, 9), sp(1, 7), 2, 2, sp(1, 3), sp(1, 18), sp(1, 21), DP_R],
  /* 3 AL */       [sp(1, 1), sp(1, 2), sp(1, 6), sp(1, 6), sp(1, 8), sp(1, 16), sp(1, 17), sp(1, 8), sp(1, 8), sp(1, 8), 3, 3, 3, sp(1, 18), sp(1, 21), DP_R],
  /* 4 EN */       [sp(1, 1), sp(1, 2), 4, sp(1, 5), sp(1, 7), sp(1, 15), sp(1, 17), sp(2, 10), 11, sp(2, 10), 4, 4, sp(1, 3), 18, 21, DP_EN],
  /* 5 AN */       [sp(1, 1), sp(1, 2), sp(1, 4), 5, sp(1, 7), sp(1, 15), sp(1, 17), sp(1, 7), sp(1, 9), sp(2, 12), 5, 5, sp(1, 3), sp(1, 18), sp(1, 21), DP_AN],
  /* 6 AL:EN/AN */ [sp(1, 1), sp(1, 2), 6, 6, sp(1, 8), sp(1, 16), sp(1, 17), sp(1, 8), sp(1, 8), sp(2, 13), 6, 6, sp(1, 3), 18, 21, DP_AN],
  /* 7 ON */       [sp(1, 1), sp(1, 2), sp(1, 4), sp(1, 5), 7, sp(1, 15), sp(1, 17), 7, sp(2, 14), 7, 7, 7, sp(1, 3), sp(1, 18), sp(1, 21), DP_ON],
  /* 8 AL:ON */    [sp(1, 1), sp(1, 2), sp(1, 6), sp(1, 6), 8, sp(1, 16), sp(1, 17), 8, 8, 8, 8, 8, sp(1, 3), sp(1, 18), sp(1, 21), DP_ON],
  /* 9 ET */       [sp(1, 1), sp(1, 2), 4, sp(1, 5), 7, sp(1, 15), sp(1, 17), 7, 9, 7, 9, 9, sp(1, 3), 18, 21, DP_ON],
  /*10 EN+ES/CS */ [sp(3, 1), sp(3, 2), 4, sp(3, 5), sp(4, 7), sp(3, 15), sp(3, 17), sp(4, 7), sp(4, 14), sp(4, 7), 10, sp(4, 7), sp(3, 3), 18, 21, DP_EN],
  /*11 EN+ET */    [sp(1, 1), sp(1, 2), 4, sp(1, 5), sp(1, 7), sp(1, 15), sp(1, 17), sp(1, 7), 11, sp(1, 7), 11, 11, sp(1, 3), 18, 21, DP_EN],
  /*12 AN+CS */    [sp(3, 1), sp(3, 2), sp(3, 4), 5, sp(4, 7), sp(3, 15), sp(3, 17), sp(4, 7), sp(4, 14), sp(4, 7), 12, sp(4, 7), sp(3, 3), sp(3, 18), sp(3, 21), DP_AN],
  /*13 AL:EN/AN+CS */ [sp(3, 1), sp(3, 2), 6, 6, sp(4, 8), sp(3, 16), sp(3, 17), sp(4, 8), sp(4, 8), sp(4, 8), 13, sp(4, 8), sp(3, 3), 18, 21, DP_AN],
  /*14 ON+ET */    [sp(1, 1), sp(1, 2), sp(4, 4), sp(1, 5), 7, sp(1, 15), sp(1, 17), 7, 14, 7, 14, 14, sp(1, 3), sp(4, 18), sp(4, 21), DP_ON],
  /*15 S */        [sp(1, 1), sp(1, 2), sp(1, 4), sp(1, 5), sp(1, 7), 15, sp(1, 17), sp(1, 7), sp(1, 9), sp(1, 7), 15, sp(1, 7), sp(1, 3), sp(1, 18), sp(1, 21), DP_S],
  /*16 AL:S */     [sp(1, 1), sp(1, 2), sp(1, 6), sp(1, 6), sp(1, 8), 16, sp(1, 17), sp(1, 8), sp(1, 8), sp(1, 8), 16, sp(1, 8), sp(1, 3), sp(1, 18), sp(1, 21), DP_S],
  /*17 B */        [sp(1, 1), sp(1, 2), sp(1, 4), sp(1, 5), sp(1, 7), sp(1, 15), 17, sp(1, 7), sp(1, 9), sp(1, 7), 17, sp(1, 7), sp(1, 3), sp(1, 18), sp(1, 21), DP_B],
  /*18 ENL */      [sp(1, 1), sp(1, 2), 18, sp(1, 5), sp(1, 7), sp(1, 15), sp(1, 17), sp(2, 19), 20, sp(2, 19), 18, 18, sp(1, 3), 18, 21, DP_L],
  /*19 ENL+ES/CS */ [sp(3, 1), sp(3, 2), 18, sp(3, 5), sp(4, 7), sp(3, 15), sp(3, 17), sp(4, 7), sp(4, 14), sp(4, 7), 19, sp(4, 7), sp(3, 3), 18, 21, DP_L],
  /*20 ENL+ET */   [sp(1, 1), sp(1, 2), 18, sp(1, 5), sp(1, 7), sp(1, 15), sp(1, 17), sp(1, 7), 20, sp(1, 7), 20, 20, sp(1, 3), 18, 21, DP_L],
  /*21 ENR */      [sp(1, 1), sp(1, 2), 21, sp(1, 5), sp(1, 7), sp(1, 15), sp(1, 17), sp(2, 22), 23, sp(2, 22), 21, 21, sp(1, 3), 18, 21, DP_AN],
  /*22 ENR+ES/CS */ [sp(3, 1), sp(3, 2), 21, sp(3, 5), sp(4, 7), sp(3, 15), sp(3, 17), sp(4, 7), sp(4, 14), sp(4, 7), 22, sp(4, 7), sp(3, 3), 18, 21, DP_AN],
  /*23 ENR+ET */   [sp(1, 1), sp(1, 2), 21, sp(1, 5), sp(1, 7), sp(1, 15), sp(1, 17), sp(1, 7), 23, sp(1, 7), 23, 23, sp(1, 3), 18, 21, DP_AN],
];
const IMPTABPROPS_RES = 15;
const sl = (a, s) => s + (a << 4);
const IMP_TAB_L = [ // even paragraph level: L, R, EN, AN, ON, S, B, Res
  [0, 1, 0, 2, 0, 0, 0, 0],
  [0, 1, 3, 3, sl(1, 4), sl(1, 4), 0, 1],
  [0, 1, 0, 2, sl(1, 5), sl(1, 5), 0, 2],
  [0, 1, 3, 3, sl(1, 4), sl(1, 4), 0, 2],
  [0, sl(2, 1), sl(3, 3), sl(3, 3), 4, 4, 0, 0],
  [0, sl(2, 1), 0, sl(3, 2), 5, 5, 0, 0],
];
const IMP_TAB_R = [ // odd paragraph level
  [1, 0, 2, 2, 0, 0, 0, 0],
  [1, 0, 1, 3, sl(1, 4), sl(1, 4), 0, 1],
  [1, 0, 2, 2, 0, 0, 0, 1],
  [1, 0, 1, 3, 5, 5, 0, 1],
  [sl(2, 1), 0, sl(2, 1), 3, 4, 4, 0, 0],
  [1, 0, 1, 3, 5, 5, 0, 0],
];
const IMPTABLEVELS_RES = 7;

function setLevelsOutsideIsolates(B, start, limit, level) {
  const dirProps = B.dirProps, levels = B.levels;
  let isolateCount = 0;
  for (let k = start; k < limit; k++) {
    const dirProp = dirProps[k];
    if (dirProp === D_PDI) isolateCount--;
    if (isolateCount === 0) levels[k] = level;
    if (dirProp === D_LRI || dirProp === D_RLI) isolateCount++;
  }
}

// impAct0 = {0,1,2,3,4}: only actions 1..4 exist in the default tables
function processPropertySeq(B, ls, prop, start, limit) {
  const impTab = ls.impTab, levels = B.levels;
  const start0 = start;
  const oldStateSeq = ls.state;
  const cell = impTab[oldStateSeq][prop];
  ls.state = cell & 0x0f;
  const actionSeq = cell >> 4;
  const addLevel = impTab[ls.state][IMPTABLEVELS_RES];
  let level;
  if (actionSeq) {
    switch (actionSeq) {
      case 1: ls.startON = start0; break;
      case 2: start = ls.startON; break;
      case 3: level = ls.runLevel + 1; setLevelsOutsideIsolates(B, ls.startON, start0, level); break;
      case 4: level = ls.runLevel + 2; setLevelsOutsideIsolates(B, ls.startON, start0, level); break;
      default: break;
    }
  }
  if (addLevel || start < start0) {
    level = ls.runLevel + addLevel;
    if (start >= ls.runStart) { for (let k = start; k < limit; k++) levels[k] = level; }
    else setLevelsOutsideIsolates(B, start, limit, level);
  }
}

function resolveImplicitLevels(B, start, limit, sor, eor) {
  const dirProps = B.dirProps;
  const ls = { impTab: null, startON: -1, runStart: start, runLevel: B.levels[start], state: 0 };
  ls.impTab = (ls.runLevel & 1) ? IMP_TAB_R : IMP_TAB_L;
  let start1, start2, stateImp;
  if (dirProps[start] === D_PDI && B.isolateCount >= 0) {
    const iso = B.isolates[B.isolateCount];
    ls.startON = iso.startON; start1 = iso.start1; stateImp = iso.stateImp; ls.state = iso.state;
    B.isolateCount--;
  } else {
    ls.startON = -1;
    start1 = start;
    stateImp = dirProps[start] === D_NSM ? 1 + sor : 0;
    ls.state = 0;
    processPropertySeq(B, ls, sor, start, start);
  }
  start2 = start;
  for (let i = start; i <= limit; i++) {
    let gprop;
    if (i >= limit) {
      let k;
      for (k = limit - 1; k > start && (DF(dirProps[k]) & MASK_BN_EXPLICIT); k--) { /* skip */ }
      const dp = dirProps[k];
      if (dp === D_LRI || dp === D_RLI) break;
      gprop = eor;
    } else {
      const prop = dirProps[i];
      if (prop === D_B) B.isolateCount = -1;
      gprop = GROUP_PROP[prop];
    }
    const oldStateImp = stateImp;
    const cell = IMP_TAB_PROPS[oldStateImp][gprop];
    stateImp = cell & 0x1f;
    let actionImp = cell >> 5;
    if (i === limit && actionImp === 0) actionImp = 1;
    if (actionImp) {
      const resProp = IMP_TAB_PROPS[oldStateImp][IMPTABPROPS_RES];
      switch (actionImp) {
        case 1: processPropertySeq(B, ls, resProp, start1, i); start1 = i; break;
        case 2: start2 = i; break;
        case 3: processPropertySeq(B, ls, resProp, start1, start2); processPropertySeq(B, ls, DP_ON, start2, i); start1 = i; break;
        case 4: processPropertySeq(B, ls, resProp, start1, start2); start1 = start2; start2 = i; break;
        default: break;
      }
    }
  }
  let i;
  for (i = limit - 1; i > start && (DF(dirProps[i]) & MASK_BN_EXPLICIT); i--) { /* skip */ }
  const dp = dirProps[i];
  if ((dp === D_LRI || dp === D_RLI) && limit < B.n) {
    B.isolateCount++;
    B.isolates[B.isolateCount] = { stateImp, state: ls.state, start1, startON: ls.startON };
  } else processPropertySeq(B, ls, eor, limit, limit);
}

// L1 and the levels of removed (X9) characters
function adjustWSLevels(B) {
  if (!(B.flags & MASK_WS)) return;
  const dirProps = B.dirProps, levels = B.levels;
  let i = B.n; // trailingWSStart of a paragraph object
  while (i > 0) {
    let flag;
    while (i > 0 && ((flag = DF(dirProps[--i])) & MASK_WS)) levels[i] = paraLevelAt(B, i);
    while (i > 0) {
      flag = DF(dirProps[--i]);
      if (flag & MASK_BN_EXPLICIT) levels[i] = levels[i + 1];
      else if (flag & MASK_B_S) { levels[i] = paraLevelAt(B, i); break; }
    }
  }
}

// ------------------------------------------------------------------------------------------------
// Segmenter state
// ------------------------------------------------------------------------------------------------
function makeState(opts) {
  const S = {
    amb: true, subMin: 0x7fffffff, subMax: -1, sub: null, repl: "�", replGid: 97, replWord: 1 | 512,
    MASK: 3, NARROW: 0, WIDE: 1, TAIL: 2, HEAD: 3, EC: 0, SC: 1, EW: 0, TW: 8,
    gList: [], gTab: new Int32Array(1024).fill(-1), gHash: new Int32Array(1024), g1: new Int32Array(0x10000).fill(-1), graphemes: [],
    kList: [""], cList: [""], kMap: new Map([["", 0]]), kTrie: { idx: 0, next: null }, sgrKeys: [""], sgrCloseKeys: [""],
    uList: [""], uMap: new Map([["", 0]]), uris: [""],
    ucKeys: new Array(UCACHE).fill(null), ucIds: new Int32Array(UCACHE),
    // per-call scratch; last validated (cells, runs) pair
    C: null, okCells: null, okRuns: null,
  };
  // ---- options (read order: ambiguousIsNarrow, substitute, replacement, screen, screen.*)
  if (opts !== undefined && opts !== null) {
    if (typeof opts !== "object" && typeof opts !== "function") throw argTypeError("Expected options to be a object for 'CellSegmenter'.");
    const amb = opts.ambiguousIsNarrow;
    if (amb !== undefined) S.amb = !!amb;
    const sub = opts.substitute;
    if (sub !== undefined && sub !== null) {
      if (!Array.isArray(sub)) throw argTypeError("Expected options.substitute to be a list of [first, last] pairs for 'CellSegmenter'.");
      const ranges = [];
      for (let i = 0; i < sub.length; i++) {
        if (!(i in sub)) throw argTypeError("Expected options.substitute[] to be a [first, last] pair for 'CellSegmenter'.");
        const pr = sub[i];
        if (!Array.isArray(pr) || pr.length < 2 || !(0 in pr) || !(1 in pr)) throw argTypeError("Expected options.substitute[] to be a [first, last] pair for 'CellSegmenter'.");
        const a = pr[0];
        if (typeof a !== "number") throw argTypeError(`The "substitute[][0]" property must be of type number. Received ${typeof a}`);
        if (Number.isInteger(a) || a !== a || !Number.isFinite(a)) {
          if (!(a >= 128 && a <= 0x10ffff)) throw rangeError(`The value of "substitute[][0]" is out of range. It must be >= 128 and <= 1114111. Received ${describeNumber(a)}`);
        } else throw argTypeError(`The "substitute[][0]" property must be of type integer. Received number`);
        const b = pr[1];
        if (typeof b !== "number") throw argTypeError(`The "substitute[][1]" property must be of type number. Received ${typeof b}`);
        if (Number.isInteger(b) || b !== b || !Number.isFinite(b)) {
          if (!(b >= a && b <= 0x10ffff)) throw rangeError(`The value of "substitute[][1]" is out of range. It must be >= ${a} and <= 1114111. Received ${describeNumber(b)}`);
        } else throw argTypeError(`The "substitute[][1]" property must be of type integer. Received number`);
        ranges.push(a, b);
        if (a < S.subMin) S.subMin = a;
        if (b > S.subMax) S.subMax = b;
      }
      if (ranges.length) S.sub = Int32Array.from(ranges);
    }
    const repl = opts.replacement;
    if (repl !== undefined) {
      if (typeof repl !== "string") throw argTypeError("Expected options.replacement to be a string for 'CellSegmenter'.");
      if (repl.length === 0) throw argTypeError("CellSegmenter: options.replacement must not be empty");
      S.repl = repl;
    }
    const scr = opts.screen;
    if (scr !== undefined && scr !== null) {
      S.MASK = toI32Field(scr.widthMask, "widthMask", 3);
      S.NARROW = toI32Field(scr.narrow, "narrow", 0);
      S.WIDE = toI32Field(scr.wide, "wide", 1);
      S.TAIL = toI32Field(scr.spacerTail, "spacerTail", 2);
      S.HEAD = toI32Field(scr.spacerHead, "spacerHead", 3);
      S.EC = toI32Field(scr.emptyCharIndex, "emptyCharIndex", 0);
      S.SC = toI32Field(scr.spacerCharIndex, "spacerCharIndex", 1);
      S.EW = toI32Field(scr.emptyWord, "emptyWord", 0);
      S.TW = toI32Field(scr.tabWidth, "tabWidth", 8);
    }
  }
  S.regular = paintIsRegular(S);
  // ---- graphemes seed: " ", "", "!".."~", "\t", replacement (deduplicated)
  const seed = [" ", ""];
  for (let c = 0x21; c <= 0x7e; c++) seed.push(String.fromCharCode(c));
  seed.push("\t");
  if (!seed.includes(S.repl)) seed.push(S.repl);
  for (const g of seed) internG(S, g);
  S.replGid = internG(S, S.repl); // already present: the lookup returns its seed id
  for (let k = 0; k < S.gList.length; k++) S.graphemes.push(S.gList[k]);
  S.C = {
    text: "", cells: null, runs: null, cellCap: 0, runCap: 0, count: 0, nRuns: 0, lastSgr: -1, lastUri: -1,
    ovf: false, prevKEnd: -1, rtl: 0, list: [], sgrIdx: 0, uriIdx: 0,
    clOpen: false, clStart: 0, clEnd: 0, clText: null, clSgr: 0, clUri: 0, clCount: 0, clSum: 0,
    clBaseW: 0, clBaseCp: 0, clEmojiBase: false, clFlags: 0, prevCls: 0, gbState: 0,
  };
  S.replWord = replacementWidth(S, S.repl) | 512;
  return S;
}

// Grapheme interning: an open-addressing table of ids keyed by the FNV-1a hash of the UTF-16 units
// (gTab: id or -1, gHash: the full hash), so a cluster is looked up straight from the line text
// without slicing it; the string is only materialised when it is new.
function gLookup(S, text, a, b, h) {
  const tab = S.gTab, hs = S.gHash, mask = tab.length - 1, len = b - a, list = S.gList;
  for (let s = (h ^ (h >>> 15)) & mask; ; s = (s + 1) & mask) {
    const id = tab[s];
    if (id < 0) return -1 - s; // miss: encode the free slot
    if (hs[s] === h) { const k = list[id]; if (k.length === len && sameChars(k, text, a)) return id; }
  }
}
function gInsert(S, str, h, slot) {
  const id = S.gList.length;
  S.gList.push(str);
  S.gTab[slot] = id; S.gHash[slot] = h;
  if (str.length === 1) S.g1[str.charCodeAt(0)] = id;
  if (2 * (id + 1) > S.gTab.length) gGrow(S);
  return id;
}
function gGrow(S) {
  const oldTab = S.gTab, oldHash = S.gHash, cap = oldTab.length * 4, mask = cap - 1;
  const tab = new Int32Array(cap).fill(-1), hs = new Int32Array(cap);
  for (let s = 0; s < oldTab.length; s++) {
    const id = oldTab[s];
    if (id < 0) continue;
    const h = oldHash[s];
    let q = (h ^ (h >>> 15)) & mask;
    while (tab[q] >= 0) q = (q + 1) & mask;
    tab[q] = id; hs[q] = h;
  }
  S.gTab = tab; S.gHash = hs;
}
function internG(S, str) {
  const h = rangeHash(str, 0, str.length) | 0;
  const r = gLookup(S, str, 0, str.length, h);
  return r >= 0 ? r : gInsert(S, str, h, -1 - r);
}
function internUnit(S, u) {
  let id = S.g1[u];
  if (id < 0) id = internG(S, String.fromCharCode(u));
  return id;
}
// Key interning through a trie keyed by entry identity (lists of canonical entries), falling back
// to the serialised key string for lists not seen yet.
function internKey(S, list) {
  const L = list.length;
  if (L === 0) return 0;
  let node = S.kTrie;
  for (let r = 0; r < L; r++) {
    const e = list[r];
    let nx = node.next === null ? undefined : node.next.get(e);
    if (nx === undefined) {
      nx = { idx: -1, next: null };
      if (node.next === null) node.next = new Map();
      node.next.set(e, nx);
    }
    node = nx;
  }
  if (node.idx < 0) node.idx = internKeyString(S, list);
  return node.idx;
}
function internKeyString(S, list) {
  let key = list[0].open, close = list[0].close;
  for (let r = 1; r < list.length; r++) { key += "\0" + list[r].open; close += "\0" + list[r].close; }
  let id = S.kMap.get(key);
  if (id === undefined) { id = S.kList.length; S.kList.push(key); S.cList.push(close); S.kMap.set(key, id); }
  return id;
}
// Per-instance direct-mapped caches from a source range text[a, b) to an interned id, so that a
// repeated cluster text or URI costs a hash and a compare instead of a substring and a Map lookup.
function rangeHash(text, a, b) {
  let h = 0x811c9dc5;
  for (let j = a; j < b; j++) h = Math.imul(h ^ text.charCodeAt(j), 16777619);
  return h >>> 0;
}
const UCACHE = 256;
function internRange(S, text, a, b) {
  const h = rangeHash(text, a, b) | 0;
  const r = gLookup(S, text, a, b, h);
  return r >= 0 ? r : gInsert(S, text.slice(a, b), h, -1 - r);
}
function internUriRange(S, text, a, b) {
  const slot = rangeHash(text, a, b) & (UCACHE - 1);
  const k = S.ucKeys[slot];
  if (k !== null && k.length === b - a && sameChars(k, text, a)) return S.ucIds[slot];
  const str = text.slice(a, b);
  const id = internUri(S, str);
  S.ucKeys[slot] = str; S.ucIds[slot] = id;
  return id;
}
function internUri(S, uri) {
  let id = S.uMap.get(uri);
  if (id === undefined) { id = S.uList.length; S.uList.push(uri); S.uMap.set(uri, id); }
  return id;
}
function mirror(list, arr) {
  for (let k = arr.length; k < list.length; k++) arr.push(list[k]);
}
// By the time segment() returns (or throws), each exposed array has had the entries
// [arr.length, count) appended; user mutation never changes ids (native mirroring semantics).
function mirrorAll(S) {
  if (S.graphemes.length !== S.gList.length) mirror(S.gList, S.graphemes);
  if (S.sgrKeys.length !== S.kList.length) mirror(S.kList, S.sgrKeys);
  if (S.sgrCloseKeys.length !== S.cList.length) mirror(S.cList, S.sgrCloseKeys);
  if (S.uris.length !== S.uList.length) mirror(S.uList, S.uris);
}
function isSub(S, cp) {
  const r = S.sub;
  for (let k = 0; k < r.length; k += 2) if (cp >= r[k] && cp <= r[k + 1]) return true;
  return false;
}

// Width of a whole string treated as one cluster (used for the replacement glyph).
// Width of the substitute glyph (measured with work/probe-repl.js over 3000 random replacement
// strings): the replacement is segmented like a line - escape sequences and lone surrogates are
// transparent, controls and TAB contribute 0, substitution does not apply - and the widths of its
// clusters are summed; the result is at least 1 and saturates at 255.
function replacementWidth(S, str) {
  const n = str.length;
  let total = 0, open = false, prevCls = 0, st = 0, cnt = 0, sum = 0, baseW = 0, baseCp = 0, eb = false, f = 0;
  const close = () => { if (open) { open = false; total += widthOf(f, cnt, sum, baseW, baseCp, eb); } };
  let i = 0;
  while (i < n) {
    const c = str.charCodeAt(i);
    if (c < 0xa0 && !(c >= 0x20 && c <= 0x7e)) {
      if (c === 0x1b || c === 0x9b || c === 0x9d || c === 0x90 || c === 0x98 || c === 0x9e || c === 0x9f) { i = escSeq(S, S.C, str, i, n, false); continue; }
      close(); i++; continue;
    }
    let cp = c, len = 1;
    if ((c & 0xf800) === 0xd800) {
      if (c <= 0xdbff && i + 1 < n) {
        const d = str.charCodeAt(i + 1);
        if ((d & 0xfc00) === 0xdc00) { cp = 0x10000 + ((c - 0xd800) << 10) + (d - 0xdc00); len = 2; }
      }
      if (len === 1) { i++; continue; }
    }
    const p = cp < 0x10000 ? P16[cp] : PROP[IDS[cp]];
    const g = (p >> 3) & 31;
    let w = (p >> 8) & 3;
    if (!S.amb && (p & P_AMB)) w = 2;
    let brk = true;
    if (open) { const d = GB[(st * NG + prevCls) * NG + g]; st = d >> 1; brk = (d & 1) !== 0; }
    else st = S_DEFAULT;
    if (brk) {
      close();
      open = true; cnt = 1; sum = w; f = p & F_MASK;
      if (w > 0) { baseW = w; baseCp = cp; eb = cp >= 0x80 && (p & P_EMOJI) !== 0; } else { baseW = 0; baseCp = 0; eb = false; }
    } else {
      cnt++; sum += w; f |= p & F_ADD;
      if (baseW === 0 && w > 0) { baseW = w; baseCp = cp; eb = cp >= 0x80 && (p & P_EMOJI) !== 0; }
    }
    prevCls = g;
    i += len;
  }
  close();
  return total < 1 ? 1 : total > 255 ? 255 : total;
}
function widthOf(f, count, sum, baseW, baseCp, emojiBase) {
  if (f & F_RI) return count >= 2 ? 2 : 1;
  if (f & F_KEYCAP) return 2;
  if (emojiBase && (f & (F_SKIN | F_ZWJ))) return 2;
  if ((f & F_VS16) && baseW === 1 && (emojiBase || baseCp === 0xa9 || baseCp === 0xae)) return sum + 1;
  return sum;
}

// ------------------------------------------------------------------------------------------------
// segment(): cluster emission
// ------------------------------------------------------------------------------------------------
// Emit one cell. gid >= 0: known grapheme id (no interning needed); otherwise the grapheme is the
// single code unit `unit` (unit >= 0) or the string `str`. kSingle: the cluster is a single
// kernel-eligible BMP code unit spanning text[a, b).
function emitCell(S, C, gid, unit, str, word, sgr, uri, kSingle, a, b) {
  const i = C.count++;
  let m = -1;
  if (i === 0 || sgr !== C.lastSgr || uri !== C.lastUri) {
    m = C.nRuns++;
    if (m >= MAX_RUNS) { C.text = ""; C.cells = null; C.runs = null; C.clText = null; mirrorAll(S); throw tooManyRuns(); }
    C.lastSgr = sgr; C.lastUri = uri;
  }
  if (!C.ovf) {
    let ok = true;
    if (m >= 0) {
      if (m >= C.runCap) ok = false;
      else { C.runs[2 * m] = sgr; C.runs[2 * m + 1] = uri; }
    }
    if (ok && i >= C.cellCap) ok = false;
    if (ok) {
      if (gid < 0) gid = unit >= 0 ? internUnit(S, unit) : str !== null ? internG(S, str) : internRange(S, C.text, a, b);
      C.cells[2 * i] = gid;
      C.cells[2 * i + 1] = word | ((C.nRuns - 1) << 10);
      C.prevKEnd = kSingle ? b : -1;
      return;
    }
    // first cell that is not written: F = i. Interned unless it is a K+ kernel unit.
    C.ovf = true;
    if (gid < 0 && !(kSingle && C.prevKEnd === a && kernelNextSafe(C.text, b))) {
      if (unit >= 0) internUnit(S, unit); else if (str !== null) internG(S, str); else internRange(S, C.text, a, b);
    }
  } else if (gid < 0 && kSingle && C.prevKEnd !== a && kernelNextSafe(C.text, b)) {
    // beyond F: only K1 kernel units are interned
    if (unit >= 0) internUnit(S, unit); else if (str !== null) internG(S, str); else internRange(S, C.text, a, b);
  }
  C.prevKEnd = kSingle ? b : -1;
}
const MAX_RUNS = 4194304; // the cell word keeps the run index in 22 bits
const tooManyRuns = () => rangeError(`CellSegmenter.segment: the line produces more than ${MAX_RUNS} style runs`);
function kernelNextSafe(text, b) {
  if (b >= text.length) return true;
  const u = text.charCodeAt(b);
  if ((u & 0xf800) === 0xd800) return false;
  return (P16[u] & P_UNSAFE) === 0;
}

function emitCluster(S, C) {
  C.clOpen = false;
  let w = widthOf(C.clFlags, C.clCount, C.clSum, C.clBaseW, C.clBaseCp, C.clEmojiBase);
  if (w === 0) return;
  if (w > 255) w = 255;
  if (C.clFlags & F_RTL) C.rtl = 1;
  const a = C.clStart, b = C.clEnd;
  if (C.clText === null && b - a === 1) {
    const u = C.text.charCodeAt(a);
    const gid = S.g1[u];
    emitCell(S, C, gid, u, null, w, C.clSgr, C.clUri, u >= 0x80 && (P16[u] & P_KERN) !== 0, a, b);
  } else {
    emitCell(S, C, -1, -1, C.clText, w, C.clSgr, C.clUri, false, a, b);
  }
}

function startCluster(S, C, i, len, cp, p) {
  if (C.sgrIdx < 0) C.sgrIdx = internKey(S, C.list);
  C.clOpen = true;
  C.clSgr = C.sgrIdx; C.clUri = C.uriIdx;
  C.clStart = i; C.clEnd = i + len; C.clText = null;
  let w = (p >> 8) & 3;
  if (!S.amb && (p & P_AMB)) w = 2;
  C.clCount = 1; C.clSum = w; C.clFlags = p & F_MASK;
  if (w > 0) { C.clBaseW = w; C.clBaseCp = cp; C.clEmojiBase = cp >= 0x80 && (p & P_EMOJI) !== 0; }
  else { C.clBaseW = 0; C.clBaseCp = 0; C.clEmojiBase = false; }
}

function addToCluster(S, C, i, len, cp, p) {
  if (C.clText !== null) C.clText += C.text.slice(i, i + len);
  else if (i !== C.clEnd) C.clText = C.text.slice(C.clStart, C.clEnd) + C.text.slice(i, i + len);
  C.clEnd = i + len;
  C.clCount++;
  let w = (p >> 8) & 3;
  if (!S.amb && (p & P_AMB)) w = 2;
  C.clSum += w;
  C.clFlags |= p & F_ADD;
  if (C.clBaseW === 0 && w > 0) { C.clBaseW = w; C.clBaseCp = cp; C.clEmojiBase = cp >= 0x80 && (p & P_EMOJI) !== 0; }
}

// A cell that is its own hard-broken cluster (TAB or a substituted code point).
function emitSingle(S, C, gid, word, a) {
  if (C.clOpen) emitCluster(S, C);
  if (C.sgrIdx < 0) C.sgrIdx = internKey(S, C.list);
  emitCell(S, C, gid, -1, null, word, C.sgrIdx, C.uriIdx, false, a, a + 1);
}

// ------------------------------------------------------------------------------------------------
// segment(): escape sequences. Return the index after the sequence. apply=false for the bidi pass.
// ------------------------------------------------------------------------------------------------
function escSeq(S, C, text, i, n, apply) {
  const c = text.charCodeAt(i);
  if (c === 0x1b) {
    const j = i + 1;
    if (j >= n) return n;
    const d = text.charCodeAt(j);
    if (d === 0x5b) return csiSeq(C, text, j + 1, n, apply);
    if (d === 0x5d) return oscSeq(S, C, text, j + 1, n, apply);
    if (d === 0x50 || d === 0x58 || d === 0x5e || d === 0x5f) return strSeq(text, j + 1, n);
    if (d >= 0x20 && d <= 0x2f) {
      if (j + 1 >= n) return n;
      const e = text.charCodeAt(j + 1);
      if (e === 0x1b) return j + 1;
      if ((e & 0xfc00) === 0xd800 && j + 2 < n && (text.charCodeAt(j + 2) & 0xfc00) === 0xdc00) return j + 3;
      return j + 2;
    }
    if (d >= 0x30 && d <= 0x7e) return j + 1;
    // CAN, SUB and ST abort the sequence and are swallowed with the ESC (measured: "a\e\x18́"
    // is one cluster); any other C0/DEL/C1/non-ASCII unit drops the ESC alone and is re-scanned.
    if (d === 0x18 || d === 0x1a || d === 0x9c) return j + 1;
    return j;
  }
  if (c === 0x9b) return csiSeq(C, text, i + 1, n, apply);
  if (c === 0x9d) return oscSeq(S, C, text, i + 1, n, apply);
  return strSeq(text, i + 1, n); // 0x90 0x98 0x9E 0x9F
}
function csiSeq(C, text, j, n, apply) {
  const start = j;
  let bad = false, inter = false;
  for (; j < n; j++) {
    const c = text.charCodeAt(j);
    if (c >= 0x30 && c <= 0x3f) { if (inter || c >= 0x3c) bad = true; continue; }
    if (c >= 0x40 && c <= 0x7e) {
      if (c === 0x6d && !bad && !inter && apply) { applySgr(C.list, text, start, j); C.sgrIdx = -1; }
      return j + 1;
    }
    if (c >= 0x20 && c <= 0x2f) { inter = true; continue; }
    if (c === 0x1b) return j;
    if (c === 0x18 || c === 0x1a || c === 0x9c) return j + 1;
    bad = true;
  }
  return n;
}
function oscSeq(S, C, text, j, n, apply) {
  const start = j;
  for (; j < n; j++) {
    const c = text.charCodeAt(j);
    let end;
    if (c === 0x07 || c === 0x9c) end = j + 1;
    else if (c === 0x1b) { if (j + 1 < n && text.charCodeAt(j + 1) === 0x5c) end = j + 2; else return j; }
    else if (c === 0x18 || c === 0x1a) return j + 1;
    else continue;
    if (apply && j - start >= 2 && text.charCodeAt(start) === 0x38 && text.charCodeAt(start + 1) === 0x3b) {
      const k = text.indexOf(";", start + 2);
      if (k >= 0 && k < j) C.uriIdx = k + 1 === j ? 0 : internUriRange(S, text, k + 1, j);
    }
    return end;
  }
  return n;
}
function strSeq(text, j, n) {
  for (; j < n; j++) {
    const c = text.charCodeAt(j);
    if (c === 0x9c) return j + 1;
    if (c === 0x1b) return j + 1 < n && text.charCodeAt(j + 1) === 0x5c ? j + 2 : j;
    if (c === 0x18 || c === 0x1a) return j + 1;
  }
  return n;
}

// ------------------------------------------------------------------------------------------------
// segment(): main pass
// ------------------------------------------------------------------------------------------------
// grapheme id of a printable ASCII character (pre-seeded): " " = 0, "!".."~" = 2..95
const ASCII_GID = new Int32Array(128);
for (let c = 0x21; c <= 0x7e; c++) ASCII_GID[c] = c - 0x1f;

function segmentPass(S, C, text) {
  const n = text.length;
  const cells = C.cells;
  let i = 0;
  while (i < n) {
    const c = text.charCodeAt(i);
    if (c >= 0x20 && c <= 0x7e) {
      // ---- printable ASCII run starting at i
      let r = i;
      if (C.clOpen) {
        const d = GB[(C.gbState * NG + C.prevCls) * NG + G_OTHER];
        C.gbState = d >> 1;
        if ((d & 1) === 0) {
          // only a Prepend absorbs the first unit; the next ASCII unit always breaks
          addToCluster(S, C, i, 1, c, P16[c]);
          r = i + 1;
          if (r >= n) { i = n; continue; }
          const d2 = text.charCodeAt(r);
          if (d2 < 0x20 || d2 > 0x7e) { C.prevCls = G_OTHER; i = r; continue; }
          emitCluster(S, C);
        } else emitCluster(S, C);
      } else C.gbState = S_DEFAULT;
      // r starts a new cluster; is r the last unit of the run?
      const c1 = r + 1 < n ? text.charCodeAt(r + 1) : -1;
      if (c1 >= 0x20 && c1 <= 0x7e) {
        // first cell of the run, attempted individually
        if (C.sgrIdx < 0) C.sgrIdx = internKey(S, C.list);
        emitCell(S, C, ASCII_GID[text.charCodeAt(r)], -1, null, 1, C.sgrIdx, C.uriIdx, false, r, r + 1);
        let u = r + 1, cu = c1;
        if (!C.ovf && C.count + (n - u) <= C.cellCap) {
          // cannot overflow: write the middle cells in one pass
          const wd = 1 | ((C.nRuns - 1) << 10);
          let o = 2 * C.count;
          for (;;) {
            const nu = u + 1 < n ? text.charCodeAt(u + 1) : -1;
            if (nu < 0x20 || nu > 0x7e) break;
            cells[o] = ASCII_GID[cu]; cells[o + 1] = wd; o += 2;
            u++; cu = nu;
          }
          C.count = o >> 1;
        } else {
          // the middle cells form one all-or-nothing block (same run as the first cell)
          let j = u + 1;
          while (j < n) { const d = text.charCodeAt(j); if (d < 0x20 || d > 0x7e) break; j++; }
          const mid = j - 1 - u, k0 = C.count;
          if (!C.ovf && k0 + mid <= C.cellCap) {
            const wd = 1 | ((C.nRuns - 1) << 10);
            let o = 2 * k0;
            for (let q = u; q < j - 1; q++) { cells[o] = ASCII_GID[text.charCodeAt(q)]; cells[o + 1] = wd; o += 2; }
          } else C.ovf = true;
          C.count = k0 + mid;
          u = j - 1; cu = text.charCodeAt(u);
        }
        C.gbState = S_DEFAULT;
        // the last unit opens a pending cluster (it may take following extenders)
        startCluster(S, C, u, 1, cu, P16[cu]);
        i = u + 1;
      } else {
        if (r > i) C.gbState = S_DEFAULT;
        startCluster(S, C, r, 1, text.charCodeAt(r), P16[text.charCodeAt(r)]);
        i = r + 1;
      }
      C.prevCls = G_OTHER;
      C.prevKEnd = -1;
      continue;
    }
    if (c < 0xa0) {
      if (c === 0x09) { emitSingle(S, C, 96, 256, i); C.prevCls = G_OTHER; i++; continue; }
      if (c === 0x1b || c === 0x9b || c === 0x9d || c === 0x90 || c === 0x98 || c === 0x9e || c === 0x9f) {
        i = escSeq(S, C, text, i, n, true);
        continue;
      }
      // hard control: boundary, nothing emitted
      if (C.clOpen) emitCluster(S, C);
      i++;
      continue;
    }
    let cp = c, len = 1;
    if ((c & 0xf800) === 0xd800) {
      if (c <= 0xdbff && i + 1 < n) {
        const d = text.charCodeAt(i + 1);
        if ((d & 0xfc00) === 0xdc00) { cp = 0x10000 + ((c - 0xd800) << 10) + (d - 0xdc00); len = 2; }
      }
      if (len === 1) { i++; continue; } // lone surrogate: skipped entirely
    }
    if (cp >= S.subMin && cp <= S.subMax && isSub(S, cp)) {
      emitSingle(S, C, S.replGid, S.replWord, i);
      C.prevCls = G_OTHER;
      i += len;
      continue;
    }
    const p = cp < 0x10000 ? P16[cp] : PROP[IDS[cp]];
    if ((p & P_KERN) !== 0 && len === 1 && kernelNextSafe(text, i + 1) &&
        (!C.clOpen || (GB[(C.gbState * NG + C.prevCls) * NG + G_OTHER] & 1) !== 0)) {
      // ---- kernel run: single-unit clusters (gcb Other, width 1-2) whose next unit cannot join
      if (C.clOpen) emitCluster(S, C);
      if (C.sgrIdx < 0) C.sgrIdx = internKey(S, C.list);
      const sgr = C.sgrIdx, uri = C.uriIdx, amb = S.amb, g1 = S.g1, subMin = S.subMin, subMax = S.subMax;
      let w = (p >> 8) & 3;
      if (!amb && (p & P_AMB)) w = 2;
      if (p & F_RTL) C.rtl = 1;
      // the first cell goes through emitCell (it may open a run or hit the capacity)
      emitCell(S, C, g1[cp], cp, null, w, sgr, uri, true, i, i + 1);
      i++;
      const rb = (C.nRuns - 1) << 10;
      let u = i < n ? text.charCodeAt(i) : 0;
      if (!C.ovf) {
        let k = C.count, lastEnd = C.prevKEnd, rtl = 0;
        const cap = C.cellCap;
        while (u >= 0xa0 && (u & 0xf800) !== 0xd800 && k < cap) {
          const pu = P16[u];
          if ((pu & P_KERN) === 0 || (u >= subMin && u <= subMax && isSub(S, u))) break;
          const nn = i + 1 < n ? text.charCodeAt(i + 1) : 0;
          if ((nn & 0xf800) === 0xd800 || (P16[nn] & P_UNSAFE) !== 0) break;
          let wu = (pu >> 8) & 3;
          if (!amb && (pu & P_AMB)) wu = 2;
          rtl |= pu & F_RTL;
          let gid = g1[u];
          if (gid < 0) gid = internUnit(S, u);
          cells[2 * k] = gid; cells[2 * k + 1] = wu | rb;
          k++; i++; lastEnd = i;
          u = nn;
        }
        C.count = k; C.prevKEnd = lastEnd;
        if (rtl) C.rtl = 1;
      } else {
        // beyond the capacity: every kernel unit goes through emitCell (interning rules)
        while (u >= 0xa0 && (u & 0xf800) !== 0xd800) {
          const pu = P16[u];
          if ((pu & P_KERN) === 0 || (u >= subMin && u <= subMax && isSub(S, u)) || !kernelNextSafe(text, i + 1)) break;
          let wu = (pu >> 8) & 3;
          if (!amb && (pu & P_AMB)) wu = 2;
          emitCell(S, C, g1[u], u, null, wu, sgr, uri, true, i, i + 1);
          i++;
          u = i < n ? text.charCodeAt(i) : 0;
        }
      }
      C.gbState = S_DEFAULT;
      C.prevCls = G_OTHER;
      continue;
    }
    const g = (p >> 3) & 31;
    if (!C.clOpen) { C.gbState = S_DEFAULT; startCluster(S, C, i, len, cp, p); }
    else {
      const d = GB[(C.gbState * NG + C.prevCls) * NG + g];
      C.gbState = d >> 1;
      if (d & 1) { emitCluster(S, C); startCluster(S, C, i, len, cp, p); }
      else addToCluster(S, C, i, len, cp, p);
    }
    C.prevCls = g;
    i += len;
  }
  if (C.clOpen) emitCluster(S, C);
}

// ------------------------------------------------------------------------------------------------
// segment(): bidi reordering (reordered=true, full success, some cell starts with an R/AL char)
// The native runs ICU over the UTF-16 "bidi text" (the visible code points: escapes, controls
// and lone surrogates removed, substituted code points as U+FFFD) and then emits each cell at the
// visual position of the first code unit of its cluster.
// ------------------------------------------------------------------------------------------------
// Second half of the reorder gate (measured): the RAW input must contain a UTF-16 code unit in the
// right-to-left script blocks - escape payloads, substituted code points and lone lead surrogates
// all count; U+200F RLM (outside the blocks) and U+FEFF do not. The first half is per cell (C.rtl:
// some written cell starts with an R/AL code point, RLM included).
function hasRtlBlockUnit(text, n) {
  for (let i = 0; i < n; i++) {
    const c = text.charCodeAt(i);
    if (c < 0x0590) continue;
    if (c <= 0x08ff || (c >= 0xfb1d && c <= 0xfdff) || (c >= 0xfe70 && c <= 0xfefe) ||
        c === 0xd802 || c === 0xd803 || c === 0xd83a || c === 0xd83b) return true;
  }
  return false;
}

// Scratch buffers for bidiReorder (segment() never re-enters itself: no user code runs inside it).
let BR_BT = new Uint16Array(256), BR_POS = new Int32Array(256), BR_VIS = new Int32Array(256), BR_RANK = new Int32Array(256),
  BR_SLOT = new Int32Array(256), BR_TMP = new Int32Array(512), BR_LV = new Uint8Array(256);
function bidiReorder(S, C, text, count) {
  const n = text.length;
  if (!hasRtlBlockUnit(text, n)) return;
  if (BR_BT.length < n) { const m = Math.max(n, 2 * BR_BT.length); BR_BT = new Uint16Array(m); BR_VIS = new Int32Array(m); BR_RANK = new Int32Array(m); BR_SLOT = new Int32Array(m); BR_LV = new Uint8Array(m); }
  if (BR_POS.length < count) { const m = Math.max(count, 2 * BR_POS.length); BR_POS = new Int32Array(m); BR_TMP = new Int32Array(2 * m); }
  const bt = BR_BT; // bidi text (never longer than the input)
  const cellPos = BR_POS; // first bidi-text unit of each cell's cluster
  let nb = 0, k = 0;
  let open = false, first = 0, prevCls = 0, st = 0, cnt = 0, sum = 0, baseW = 0, baseCp = 0, eb = false, f = 0;
  // "close the pending cluster" is written out at each site: a closure over these locals is slow on V8
  let i = 0;
  while (i < n) {
    const c = text.charCodeAt(i);
    if (c < 0xa0 && !(c >= 0x20 && c <= 0x7e)) {
      if (c === 0x1b || c === 0x9b || c === 0x9d || c === 0x90 || c === 0x98 || c === 0x9e || c === 0x9f) { i = escSeq(S, C, text, i, n, false); continue; }
      if (open) { open = false; if (k < count && widthOf(f, cnt, sum, baseW, baseCp, eb) > 0) cellPos[k++] = first; }
      if (c === 0x09) { if (k < count) cellPos[k++] = nb; bt[nb++] = 9; }
      i++; continue;
    }
    let cp = c, len = 1;
    if ((c & 0xf800) === 0xd800) {
      if (c <= 0xdbff && i + 1 < n) {
        const d = text.charCodeAt(i + 1);
        if ((d & 0xfc00) === 0xdc00) { cp = 0x10000 + ((c - 0xd800) << 10) + (d - 0xdc00); len = 2; }
      }
      if (len === 1) { i++; continue; }
    }
    if (cp >= S.subMin && cp <= S.subMax && isSub(S, cp)) {
      if (open) { open = false; if (k < count && widthOf(f, cnt, sum, baseW, baseCp, eb) > 0) cellPos[k++] = first; }
      if (k < count) cellPos[k++] = nb; bt[nb++] = 0xfffd;
      prevCls = 0; i += len; continue;
    }
    const p = cp < 0x10000 ? P16[cp] : PROP[IDS[cp]];
    const g = (p >> 3) & 31;
    let w = (p >> 8) & 3;
    if (!S.amb && (p & P_AMB)) w = 2;
    let brk = true;
    if (open) { const d = GB[(st * NG + prevCls) * NG + g]; st = d >> 1; brk = (d & 1) !== 0; }
    else st = S_DEFAULT;
    if (brk) {
      if (open && k < count && widthOf(f, cnt, sum, baseW, baseCp, eb) > 0) cellPos[k++] = first;
      open = true; first = nb; cnt = 1; sum = w; f = p & F_MASK;
      if (w > 0) { baseW = w; baseCp = cp; eb = cp >= 0x80 && (p & P_EMOJI) !== 0; } else { baseW = 0; baseCp = 0; eb = false; }
    } else {
      cnt++; sum += w; f |= p & F_ADD;
      if (baseW === 0 && w > 0) { baseW = w; baseCp = cp; eb = cp >= 0x80 && (p & P_EMOJI) !== 0; }
    }
    prevCls = g;
    bt[nb++] = c;
    if (len === 2) bt[nb++] = text.charCodeAt(i + 1);
    i += len;
  }
  if (open && k < count && widthOf(f, cnt, sum, baseW, baseCp, eb) > 0) cellPos[k++] = first;
  if (k !== count || nb === 0) return; // cannot happen; never corrupt the output
  const lv = BR_LV;
  lv.set(icuLevels(bt.subarray(0, nb), nb));
  // L2 over the code units
  const vis = BR_VIS;
  let maxLv = 0;
  for (let q = 0; q < nb; q++) { vis[q] = q; if (lv[q] > maxLv) maxLv = lv[q]; }
  for (let l = maxLv; l >= 1; l--) {
    let a = 0;
    while (a < nb) {
      if (lv[a] >= l) {
        let b = a + 1;
        while (b < nb && lv[b] >= l) b++;
        for (let x = a, y = b - 1; x < y; x++, y--) {
          const t = vis[x]; vis[x] = vis[y]; vis[y] = t;
          const tl = lv[x]; lv[x] = lv[y]; lv[y] = tl;
        }
        a = b;
      } else a++;
    }
  }
  const rank = BR_RANK, slot = BR_SLOT;
  for (let x = 0; x < nb; x++) { rank[vis[x]] = x; slot[x] = -1; }
  for (let q = 0; q < count; q++) slot[rank[cellPos[q]]] = q;
  const cells = C.cells, tmp = BR_TMP;
  for (let q = 0; q < 2 * count; q++) tmp[q] = cells[q];
  let o = 0;
  for (let x = 0; x < nb; x++) {
    const ci = slot[x];
    if (ci >= 0) { cells[o++] = tmp[2 * ci]; cells[o++] = tmp[2 * ci + 1]; }
  }
}

// ------------------------------------------------------------------------------------------------
// The class
// ------------------------------------------------------------------------------------------------
class Impl {
  #s;
  constructor(options) { this.#s = makeState(options); }
  static state(self) {
    if (self === null || (typeof self !== "object" && typeof self !== "function") || !(#s in self)) throw invalidThis(self);
    return self.#s;
  }
  static getterState(self, name) {
    if (self === null || (typeof self !== "object" && typeof self !== "function") || !(#s in self))
      throw new TypeError(`The CellSegmenter.${name} getter can only be used on instances of CellSegmenter`);
    return self.#s;
  }
  get graphemes() { return Impl.getterState(this, "graphemes").graphemes; }
  paint(screen, screenWidth, x, y, cells, count, order, charMap, runWords) {
    const S = Impl.state(this);
    return paintImpl(S, screen, screenWidth, x, y, cells, count, order, charMap, runWords);
  }
  segment(text, cells, runs, reordered) {
    const S = Impl.state(this);
    if (typeof text !== "string") throw argTypeError("Expected line to be a string for 'segment'.");
    if (cells !== S.okCells || runs !== S.okRuns) {
      // a validated fixed-length Int32Array stays valid, so the (cells, runs) pair is cached
      if (!isI32(cells)) throw i32Error("cells");
      if (!isI32(runs)) throw i32Error("runs");
      if (overlaps(cells, runs)) throw argTypeError("segment: cells and runs must not overlap");
      S.okCells = cells; S.okRuns = runs;
    }
    const C = S.C;
    C.text = text; C.cells = cells; C.runs = runs;
    C.cellCap = cells.length >>> 1; C.runCap = runs.length >>> 1;
    C.count = 0; C.nRuns = 0; C.lastSgr = -1; C.lastUri = -1; C.ovf = false; C.prevKEnd = -1; C.rtl = 0;
    if (C.list.length !== 0) C.list.length = 0;
    C.sgrIdx = 0; C.uriIdx = 0;
    C.clOpen = false; C.clText = null; C.prevCls = 0; C.gbState = 0;
    segmentPass(S, C, text);
    const count = C.count;
    let ret = count;
    if (C.ovf) ret = -count;
    else if (reordered && C.rtl && count > 1) bidiReorder(S, C, text, count);
    C.text = ""; C.cells = null; C.runs = null; C.clText = null;
    mirrorAll(S);
    return ret;
  }
  setCell(screen, screenWidth, x, y, charId, word) {
    const S = Impl.state(this);
    return setCellImpl(S, screen, screenWidth, x, y, charId, word);
  }
  get sgrCloseKeys() { return Impl.getterState(this, "sgrCloseKeys").sgrCloseKeys; }
  get sgrKeys() { return Impl.getterState(this, "sgrKeys").sgrKeys; }
  get uris() { return Impl.getterState(this, "uris").uris; }
}

// ------------------------------------------------------------------------------------------------
// paint()
// ------------------------------------------------------------------------------------------------
function overlaps(a, b) {
  return a.buffer === b.buffer && a.byteLength > 0 && b.byteLength > 0 &&
    a.byteOffset < b.byteOffset + b.byteLength && b.byteOffset < a.byteOffset + a.byteLength;
}
function paintImpl(S, screen, screenWidth, x, y, cells, count, order, charMap, runWords) {
  // validation order (measured): numbers, then the arrays in argument order, then sizes, overlap
  const W = toU32Arg(screenWidth, "screenWidth");
  x = toInt32(x); y = toInt32(y);
  count = toU32Arg(count, "count");
  if (!isI32(screen)) throw i32Error("screen");
  if (!isI32(cells)) throw i32Error("cells");
  if (order === null) order = undefined;
  if (order !== undefined && !isI32(order)) throw i32Error("order");
  if (!isI32(charMap)) throw i32Error("charMap");
  if (!isI32(runWords)) throw i32Error("runWords");
  if (2 * count > cells.length) throw argTypeError("paint: count exceeds cells");
  if (order !== undefined) {
    if (count > order.length) throw argTypeError("paint: count exceeds order");
    for (let k = 0; k < count; k++) if (order[k] < 0 || order[k] >= count) throw argTypeError("paint: order index out of range");
  }
  if (overlaps(screen, cells) || overlaps(screen, charMap) || overlaps(screen, runWords) || (order !== undefined && overlaps(screen, order)))
    throw argTypeError("paint: screen must not overlap the input arrays");
  if (order === undefined) order = null;
  if (!S.regular) return paintPerWrite(S, screen, W, x, y, cells, count, order, charMap, runWords);
  const MASK = S.MASK, NARROW = S.NARROW, WIDE = S.WIDE, TAIL = S.TAIL, HEAD = S.HEAD, EC = S.EC, SC = S.SC, EW = S.EW, TW = S.TW;
  const rows = W > 0 ? Math.floor(Math.floor(screen.length / 2) / W) : 0;
  const rowOk = y >= 0 && y < rows;
  const base = rowOk ? 2 * y * W : 0;
  const cmLen = charMap.length, rwLen = runWords.length;
  let lo = -1, hi = -1, tS = 0, tE1 = 0; // written span [lo, hi]; pre-paint types of lo and hi
  let col = x;
  for (let k = 0; k < count; k++) {
    const idx = order === null ? k : order[k];
    const g = cells[2 * idx], f = cells[2 * idx + 1];
    if (f & 256) {
      if (col < W) {
        // tabWidth 0 (constructor-accepted) advances one column (measured: c10.tabwidth0)
        let stop = TW > 0 ? col + (TW - (col % TW)) : col + 1;
        if (stop > W) stop = W;
        if (rowOk) {
          for (let c = col < 0 ? 0 : col; c < stop; c++) {
            const o = base + 2 * c;
            if (c > hi) { if (lo < 0) { lo = c; tS = screen[o + 1] & MASK; } tE1 = screen[o + 1] & MASK; hi = c; }
            screen[o] = EC; screen[o + 1] = EW | NARROW;
          }
        }
        col = stop;
      }
      continue;
    }
    const width = f & 255;
    const run = f >>> 10;
    const rw = run < rwLen ? runWords[run] : EW;
    const ch = g >= 0 && g < cmLen ? charMap[g] : EC;
    if (width >= 2 && col + width > W) {
      if (rowOk && col >= 0 && col < W) {
        const o = base + 2 * col;
        if (col > hi) { if (lo < 0) { lo = col; tS = screen[o + 1] & MASK; } tE1 = screen[o + 1] & MASK; hi = col; }
        screen[o] = EC; screen[o + 1] = EW | HEAD;
      }
      col += 1;
      continue;
    }
    if (width <= 1) {
      if (rowOk && col >= 0 && col < W) {
        const o = base + 2 * col;
        if (col > hi) { if (lo < 0) { lo = col; tS = screen[o + 1] & MASK; } tE1 = screen[o + 1] & MASK; hi = col; }
        screen[o] = ch; screen[o + 1] = (rw & ~MASK) | NARROW;
      }
      col += width;
      continue;
    }
    if (rowOk) {
      for (let j = col >= 0 ? 0 : 2; j < width; j++) {
        const c = col + j;
        if (c < 0 || c >= W) continue;
        const o = base + 2 * c;
        // A head clipped at the left edge (col < 0) whose extra tails land on screen is the first
        // writer: the left orphan rule is keyed to the cell's start column (negative), so it never
        // fires (measured: a width-3 cell at x=-1 over WIDE,TAIL at 0,1 keeps column 0).
        if (c > hi) { if (lo < 0) { lo = c; tS = col < 0 ? -1 : screen[o + 1] & MASK; } tE1 = screen[o + 1] & MASK; hi = c; }
        if (j === 0) { screen[o] = ch; screen[o + 1] = (rw & ~MASK) | WIDE; }
        else if (j === 1) { screen[o] = SC; screen[o + 1] = EW | TAIL; }
        else { screen[o] = SC; screen[o + 1] = (rw & ~MASK) | TAIL; }
      }
    }
    col += width;
  }
  let dS = 65535, dE = 0;
  if (lo >= 0) {
    const s = lo, e = hi + 1;
    dS = s; dE = e;
    if (s > 0 && tS === TAIL && (screen[base + 2 * (s - 1) + 1] & MASK) === WIDE) {
      screen[base + 2 * (s - 1)] = EC; screen[base + 2 * (s - 1) + 1] = EW; dS = s - 1;
    }
    if (e < W && tE1 === WIDE && (screen[base + 2 * e + 1] & MASK) === TAIL) {
      screen[base + 2 * e] = EC; screen[base + 2 * e + 1] = EW; dE = e + 1;
    }
    if (dS > 65535) dS = 65535;
    if (dE > 65535) dE = 65535;
  }
  const low = col < 0 ? 0 : col > 1048575 ? 1048575 : col;
  return dE * 68719476736 + dS * 1048576 + low;
}

// ------------------------------------------------------------------------------------------------
// setCell()
// ------------------------------------------------------------------------------------------------
// paint() is, natively, a sequence of setCell-style writes: every written cell goes through the same
// primitive as setCell (write; a WIDE-typed word also writes the spacer tail [SC, EW|TAIL] at c+1;
// then the left/right orphan checks on the CURRENT row state). With a "regular" screen
// configuration - every word paint can write has an unambiguous type - that sequence collapses to
// the edge-only rules of paint/spec.md, which is what paintImpl's fast loop implements. Other
// configurations (measured with fuzz-paint.js: e.g. widthMask hiding the wide bit, spacerTail ===
// wide) take paintPerWrite, a literal per-write model.
function paintIsRegular(S) {
  const M = S.MASK, W = S.WIDE, T = S.TAIL;
  const types = [S.NARROW & M, (S.EW | S.NARROW) & M, (S.EW | S.HEAD) & M, S.EW & M]; // narrow, tab, spacer head, cleared
  if ((W & M) !== W || (T & M) !== T || ((S.EW | T) & M) !== T || W === T) return false;
  for (const x of types) if (x === W || x === T) return false;
  return true;
}
function paintPerWrite(S, screen, W, x, y, cells, count, order, charMap, runWords) {
  const MASK = S.MASK, NARROW = S.NARROW, WIDE = S.WIDE, TAIL = S.TAIL, HEAD = S.HEAD, EC = S.EC, SC = S.SC, EW = S.EW, TW = S.TW;
  const rows = W > 0 ? Math.floor(Math.floor(screen.length / 2) / W) : 0;
  const rowOk = y >= 0 && y < rows;
  const base = rowOk ? 2 * y * W : 0;
  const cmLen = charMap.length, rwLen = runWords.length;
  let dS = 65535, dE = 0;
  const put = (c, ch, wd) => { // setCellImpl's write + orphan rules, damage folded into [dS, dE)
    if (!rowOk || c < 0 || c >= W) return;
    const t = wd & MASK;
    const wideTail = t === WIDE && c + 1 < W;
    const e = wideTail ? c + 2 : c + 1;
    const o = base + 2 * c;
    const tS = screen[o + 1] & MASK, tSm1 = c > 0 ? screen[o - 1] & MASK : -1;
    const tEm1 = screen[base + 2 * (e - 1) + 1] & MASK, tE = e < W ? screen[base + 2 * e + 1] & MASK : -1;
    screen[o] = ch; screen[o + 1] = wd;
    if (wideTail) { screen[o + 2] = SC; screen[o + 3] = EW | TAIL; }
    let s0 = c, e0 = e;
    if (t !== TAIL && c > 0 && tS === TAIL && tSm1 === WIDE) { screen[o - 2] = EC; screen[o - 1] = EW; s0 = c - 1; }
    if (e < W && tEm1 === WIDE && tE === TAIL) { screen[base + 2 * e] = EC; screen[base + 2 * e + 1] = EW; e0 = e + 1; }
    if (s0 < dS) dS = s0;
    if (e0 > dE) dE = e0;
  };
  let col = x;
  for (let k = 0; k < count; k++) {
    const idx = order === null ? k : order[k];
    const g = cells[2 * idx], f = cells[2 * idx + 1];
    if (f & 256) {
      if (col < W) {
        let stop = TW > 0 ? col + (TW - (col % TW)) : col + 1;
        if (stop > W) stop = W;
        for (let c = col < 0 ? 0 : col; c < stop; c++) put(c, EC, EW | NARROW);
        col = stop;
      }
      continue;
    }
    const width = f & 255;
    const run = f >>> 10;
    const rw = run < rwLen ? runWords[run] : EW;
    const ch = g >= 0 && g < cmLen ? charMap[g] : EC;
    if (width >= 2 && col + width > W) { put(col, EC, EW | HEAD); col += 1; continue; }
    if (width <= 1) { put(col, ch, (rw & ~MASK) | NARROW); col += width; continue; }
    if (col >= 0) put(col, ch, (rw & ~MASK) | WIDE);
    for (let j = 2; j < width; j++) put(col + j, SC, (rw & ~MASK) | TAIL);
    col += width;
  }
  if (dS > 65535) dS = 65535;
  if (dE > 65535) dE = 65535;
  const low = col < 0 ? 0 : col > 1048575 ? 1048575 : col;
  return dE * 68719476736 + dS * 1048576 + low;
}

function setCellImpl(S, screen, screenWidth, x, y, charId, word) {
  const W = toU32Arg(screenWidth, "screenWidth");
  x = toInt32(x); y = toInt32(y);
  const ch = toInt32(charId), wd = toInt32(word);
  if (!isI32(screen)) throw i32Error("screen");
  const MASK = S.MASK, WIDE = S.WIDE, TAIL = S.TAIL, EC = S.EC, SC = S.SC, EW = S.EW;
  const rows = W > 0 ? Math.floor(Math.floor(screen.length / 2) / W) : 0;
  let lo = x + 1;
  if (lo < 0) lo = 0; else if (lo > 1048575) lo = 1048575;
  if (y < 0 || y >= rows || x < 0 || x >= W) return 65535 * 1048576 + lo;
  const base = 2 * y * W;
  const T = (c) => screen[base + 2 * c + 1] & MASK;
  const t = wd & MASK;
  const s = x;
  let e = x + 1;
  const wideTail = t === WIDE && x + 1 < W;
  if (wideTail) e = x + 2;
  const tS = T(s), tSm1 = s > 0 ? T(s - 1) : -1;
  const tEm1 = T(e - 1), tE = e < W ? T(e) : -1;
  screen[base + 2 * x] = ch; screen[base + 2 * x + 1] = wd;
  if (wideTail) { screen[base + 2 * x + 2] = SC; screen[base + 2 * x + 3] = EW | TAIL; }
  let ds = s, de = e;
  if (t !== TAIL && s > 0 && tS === TAIL && tSm1 === WIDE) { screen[base + 2 * (s - 1)] = EC; screen[base + 2 * (s - 1) + 1] = EW; ds = s - 1; }
  if (e < W && tEm1 === WIDE && tE === TAIL) { screen[base + 2 * e] = EC; screen[base + 2 * e + 1] = EW; de = e + 1; }
  return Math.min(de, 65535) * 68719476736 + Math.min(ds, 65535) * 1048576 + lo;
}

// ------------------------------------------------------------------------------------------------
// Public constructor: a plain function (so that calling it without `new` gives the native error)
// that builds an Impl-branded object with the caller's prototype.
// ------------------------------------------------------------------------------------------------
function CellSegmenter() {
  if (new.target === undefined)
    throw nodeError(TypeError, "ERR_ILLEGAL_CONSTRUCTOR", "CellSegmenter constructor cannot be invoked without 'new'");
  return Reflect.construct(Impl, [arguments[0]], new.target);
}
{
  const src = Impl.prototype;
  const proto = {};
  const desc = (k) => Object.getOwnPropertyDescriptor(src, k);
  for (const k of ["graphemes", "paint", "segment", "setCell", "sgrCloseKeys", "sgrKeys", "uris"]) {
    const d = desc(k);
    if (d.get) Object.defineProperty(proto, k, { get: d.get, set: undefined, enumerable: true, configurable: false });
    else Object.defineProperty(proto, k, { value: d.value, writable: true, enumerable: true, configurable: false });
  }
  Object.defineProperty(proto, "constructor", { value: CellSegmenter, writable: true, enumerable: false, configurable: true });
  Object.defineProperty(proto, Symbol.toStringTag, { value: "CellSegmenter", writable: false, enumerable: false, configurable: true });
  Object.defineProperty(CellSegmenter, "prototype", { value: proto, writable: false, enumerable: false, configurable: false });
}

// Install as target.ant.CellSegmenter unless a native one is present. Returns true if installed.
function installBunAnt(target) {
  if (target === undefined) target = globalThis.Bun;
  if (target === null || (typeof target !== "object" && typeof target !== "function")) return false;
  let ant = target.ant;
  if (ant !== null && ant !== undefined && typeof ant.CellSegmenter === "function") return false;
  if (ant === null || (typeof ant !== "object" && typeof ant !== "function")) {
    ant = {};
    try { Object.defineProperty(target, "ant", { value: ant, writable: true, enumerable: true, configurable: true }); }
    catch { target.ant = ant; }
    if (target.ant !== ant) return false;
  }
  Object.defineProperty(ant, "CellSegmenter", { value: CellSegmenter, writable: true, enumerable: true, configurable: true });
  return true;
}

export { CellSegmenter, installBunAnt };
export default CellSegmenter;
